# Mappa funzionale — Syntesis-ICP

> **Versione software mappata:** 8.111.0 (8.111.0 Vedere apre **ogni file con impostazioni predefinite** (richiesta utente): in `addLayer` il colore passa da ciclato (`DEFAULT_PALETTE`) a costante `VEDERE_OPEN_COLOR = #FF8533` e le **mesh** partono in render-mode **`both`** ("Entrambi" = solido+reticolo, overlay creato al caricamento) invece di `solid` (pointcloud restano `solid`). Il picker per-oggetto e "Ripristina" restano. Vedere v8.0.8->v8.0.9-refactor. 8.110.0 Vedere apre STL/PLY da **URL esterno** via deep-link `?url=<link>&file_name=` — ramo aggiunto a `autoLoadFromURL` (Blocco 5r) accanto al `?file_id=` di Drive: `fetch` client-side → `loadFile` (stesso percorso del drag-drop); usato da **ac-ordini** per aprire l'STL paziente da Supabase Storage pubblico con un click; sicurezza: solo `https` host `*.supabase.co`, CORS bucket pubblico verificato. Vedere v8.0.7->v8.0.8-refactor. 8.109.1 DISABILITA temporaneamente la voce File > "Area personale" (Dashboard) su Analizza v3b e Vedere — feature non pronta, richiesta utente — via regola CSS `#fmDashboardItem{display:none!important}` che batte l'inline messo da `ds/syn-auth-ui.js` (modulo gated NON toccato); ANALIZZA_BUILD 8.100.1->8.109.1, Vedere v8.0.6->v8.0.7-refactor. 8.109.0 REDESIGN `/gestione` look "Cockpit tecnico": i 4 TAB admin diventano SIDEBAR scura ink-blu (`.sb`/`.sb-item`, brand+nav+chip Amministrazione) + `.main` con topbar sottile; `switchAdminTab` ora seleziona `.sb-item` (`data-pane` invariati); re-skin completo via `:root` superset — accent BLU Synthesis #0065B3 (no teal), header tabella scuri, pill sature, KPI peso 700 — colori-asse anteprima STL 3D preservati; inventario id/funzioni IDENTICO all'originale (logica JS intatta), `.admin-tabs`/`.atab` = CSS legacy morto; solo frontend gestione, NON tocca v3b/backend. 8.100.1 FIX errore "opacity su undefined" pescato dal session log: guardia if(!m||!m.material) in misICP_applyLayerOp (slider albero Misurare); i 2 errori Sostituire erano gia' guardati. 8.100.0 SESSION LOG "stella polare": cattura completa front (wrapper fetch + delega click/change + console.error/warn + errori) e backend (buffer RAM + middleware richieste + handler warning/error, GET /api/logs require_authorized), REDAZIONE sicurezza a monte (password/token/JWT/segreti/URL-cred mai loggati), pulsante VISIBILE "⤓ log" .expert-only -> synLogDownloadAll scarica front+backend in un .txt. 8.99.1 FIX Raffina p2plane: Method C seedato col CENTRO DISCO p.discWorld invece che con p.position=connessione (~5mm sotto) -> non piu' no-fit su ogni marker; ora il p2plane applica davvero. 8.99.0 RAFFINA p2plane = DEFAULT: flip del default motore Raffina Sostituire da balanced a p2plane (Method C), su richiesta utente; pavimento di sicurezza per-marker (fallback Kabsch dove Method C non applica), supportato da mcApplied=ok sui 6 SR reali; sostAlignAll+switchSettingsTab ri-baselinati). 8.98.0 RAFFINA PUNTO-A-PIANO (Method C) opt-in: nuovo motore Raffina Sostituire "p2plane" che instrada la Raffina attraverso _sostMethodCPose (p2plane semantico gia' validato) -> disaccoppia disco/cilindro; 3° radio Impostazioni->Algoritmo, flag syntesis_sost_raffina=p2plane, default INVARIATO; helper _sostRaffDiag + 7 colonne CSV post-Raffina; sostAlignAll+sostDownloadDiagLog ri-baselinati). 8.97.0 RAFFINA BILANCIATA default (peso cap=nBody/nCap invece di 5x fisso -> Sigma_cap=Sigma_parete): migliora centraggio SR/1T3 tenendo il top; A/B radio Impostazioni->Algoritmo, flag syntesis_sost_raffina; sostAlignAll+switchSettingsTab ri-baselinati). 8.96.0 DEFAULT centraggio Sostituire = ROBUST click-invariante (era legacy; synSostCenterRead + syn-env init pannello + radio; fail-soft a legacy; richiesta utente, difetto cilindro-vs-disco-top che Raffina peggiorava). Precedenti patch: 8.95.3 dead-code -3 fn (_sostLocalWallAxis/sostTogglePlacedVisibility/toggleAllSB, SOST_FNS 47->45); 8.95.2 fix posa Sostituire in vista reticolo/both (raycast non-ricorsivo); 8.95.1 diagnostica synLog Sostituire. Base 8.95.0 (MODULARIZZAZIONE F6f 3/3 — **CHIUDE LA FASE 6**: MISURARE viz 3D/albero fuori dal monolite — 23 fn del dominio §MISURARE-VIZ (label 3D HTML tracker, cutview 2D perpVectors/sliceByPlane/projectTo2D/drawCutview, albero scena CATIA show/hide/toggle/group/layer-vis-op/badge/reset) → `wf/misurare-viz.js` (ottavo file wf/; STATO misICP_labels/labelsVisible/layerColors/cutZoom + banner §MISURARE-VIZ restano). Con questo il dominio mis ha **0 funzioni nel monolite** (tutte 123 estratte: 59 icp + 41 pdf + 23 viz). **FASE 6 COMPLETA**: 8 file wf/ (fresabilita/tree/report-analizza/sostituire/replace/misurare-icp/misurare-pdf/misurare-viz), tutti i workflow fuori dal monolite. Monolite 7.205 → 6.711 righe (da 41.480 iniziali = **-84%**). F6f 2/3: 41 fn report PDF/Excel → `wf/misurare-pdf.js` (residuo `_synScaricoConoImg`; aggiornato `scripts/gate/report/gate.mjs`). F6f 1/3: 59 fn §MISURARE-ICP → `wf/misurare-icp.js` (**golden-master numerico headless verde** su 4 fixtures incl. multi-scanbody 5x). F6e: 92 fn REPLACE-IT → `wf/replace.js`. F6d: 47 fn SOSTITUIRE → `wf/sostituire.js`; F6c: 4 fn report Analizza → `wf/report-analizza.js`; F6b: 10 fn tree → `wf/tree.js`; F6a: 34 fn FRESABILITA → `wf/fresabilita.js`. Base 8.87.x redirect+testi con-h; 8.86.0 F5; 8.85.0 F4; 8.84.0-.2 F1-F3. — **Data:** 2026-07-06
> **Generata dal codice reale, verificata per riga.** Ogni voce cita il file e la riga di provenienza. Dove un dettaglio non è verificabile è marcato **DA CHIARIRE**, non inventato.
> **Stato documento:** completo — tutte e 5 le viste coperte.
> **Design system (8.60.0–8.61.0, Fase pastello):** la UI usa token CSS. I token **condivisi** `--blue/--green/--red/--gold` (in `:root` v3b L40 + `ds/tokens.css` + `:root` JS-iniettato) restano **saturi** e servono testo/accenti **e le letture cliniche** (`.divergence-label` → token `--clin-*` = palette d3 canonica dal 8.60.0; `.angle-val.good/.warn/.bad`, avvisi sottosquadro/fresabilità, bordi `.clinical-section`). Dal **8.61.0** i soli **sfondi dei pulsanti/CTA** (~26) usano token **FILL pastello** dedicati `--fill-primary:#4FA3E3 / --fill-confirm:#8ADFB2 / --fill-warn:#FFE08A / --fill-error:#FF8D85 / --fill-sel:#7DBDF2` con **testo scuro** `var(--dark)` (contrasto AA). Quindi: pulsante pastello = `background:var(--fill-*)` + `color:var(--dark)`; testo/accenti/clinici = colori saturi. Mesh scansione → `#DCE6EC` rinviata (commit 3). **8.74.0 (Fase B, commit 1 — unificazione chrome):** il pannello **Misurare** entra nel sistema FILL (via i gradienti pre-pastello: `.mis-btn-run` blu `#0077CC/#0050A0` → `--fill-primary`+`--dark`; modalità click-seed viola Tailwind `#8B5CF6/#7C3AED/#C4B5FD/#F5F3FF/#5B21B6` → famiglia **selezione** `--fill-sel`/`--pearl`/`--blue`; Annulla → `--pearl`+bordo); login in-app idem → `--fill-primary`; cutview `#1a1a22` → `var(--dark)`; danger `#E24B4A/#A32D2D` → `--red`/`--fill-error` (`.btn.danger`, hover del-btn dark). **CTA di pannello unificate** alla specifica di Sostituire (`padding:8px 10px; font-size:12px; letter-spacing:.03em`): i 5 bottoni di `#replaceFlow` allineati; `#sostBtnExport` passa da nero pieno a classe **`.export-btn`** (= Accoppia). Nuova classe **`.syn-select`** (bordo/raggio/font unici) sui 5 select di `#panelReplace` + `#misSeedType`. Canvas/PDF e palette-dati (gruppi, sfondo ambiente) NON toccati.
> **Coerenza cross-superficie (8.61.2):** il menù **WorkFlow di Vedere** (`syntesis-icp-vedere.html`, file separato) elencava 4 workflow → aggiunta la voce **Replace-iT** (`data-wf="replace"`, markup+icona clonati dal monolite); `selectWorkflow` di Vedere ora naviga a `/analizzare?wf=<wf>` (deep-link passato, prima andava a `/analizzare` default = Analizza) e gestisce `replace`. Nel monolite la whitelist deep-link `?wf=` ([~5201](../backend/static/syntesis-analyzer-v3b.html#L5201)) ora include `'replace'`. Decisione architetturale (utente): **Opzione A** = 2 file separati ma deriva azzerata (menù+stile+comportamenti uniformi); unificazione fisica/modularizzazione = direzione a lungo termine. **8.79.0 (Fase C — coerenza cross-pagine):** `ds/tokens.css` completato (`--syn-ghost`, palette clinica `--clin-*`, palette **Mario** centralizzata — era duplicata identica nei `:root` di Vedere e Dashboard, ora rimossa dai locali) e **linkato da tutte le pagine satellite** (home/accedi/dashboard/gestione; Vedere già linkava). **v3b resta self-contained** per scelta: il suo `:root` inline è il canone, tokens.css lo specchia. I `:root` locali restanti sono alias/temi deliberati (home dark: `--blue #2E9BE6` soft per lo splash scuro; accedi/gestione: alias semantici `--accent/--ink` con valori canonici). **Brand "Synthesis" (con h, direzione documentata)** su superfici visibili: `<title>` di tutte le pagine, wordmark topbar gestione, testi UI (accedi/dashboard), intestazioni e footer PDF di v3b (×22); NON toccati: cartella Drive `Syntesis-ICP` (nome letterale su Drive), nomi file scaricati (`SyntesisICP_*`), tag di log, identificatori interni, repo/domini. Vedere → v8.0.3-refactor. **8.75.0 — Albero Scena unificato (feedback utente con screenshot: "lo chiamiamo in due modi e lo gestiamo in due modi")**: un solo NOME ovunque — il pannello di Vedere passa da "Layer" a **"Albero Scena"** (`.lp-title`, colore titolo allineato a v3b `var(--gray)`; "Rimuovi layer" → "Rimuovi dalla scena") e in v3b il pulsante toolbar passa da "Livelli" a **"Albero Scena"** (2 varianti, title "Albero Scena (L)"); una sola AFFORDANCE di visibilità — in Vedere l'**occhio** delle righe (`.slp-eye`+SynIcons.eye) diventa **checkbox** nativo (`.slp-vis`, `accent-color:var(--blue)`, stesso `data-action="toggle-visible"` sulla delega esistente) come i 3 alberi di v3b. Restano differenze FUNZIONALI dichiarate (azioni per-riga Centra/Isola/Rimuovi solo in Vedere; slider opacità solo in v3b) — passo successivo se richiesto. Vedere → v8.0.2-refactor. **8.77.0 (Fase D — pulizia dedicata, nessun elemento visibile toccato)**: rimossi il pannello **Scene Registry** nascosto dal Blocco 7a (`#synLayerPanel`: markup, CSS `.syn-layer-panel`/`.slp-*`, IIFE "Fase 3" — le sue funzioni erano confluite nell'Albero Scena; Synthesis Core `SynRegistry`/`SynIcons` RESTA, è usato dai bridge loadScan/placeMUA), i **CSS orfani del Comparator** (`.acc-*`, `.misurare-btn/-header/-count/-refresh/-selected`, `.slot*`; `comp-radio`+`.sw` vivi nei radio scanbody), i **console.log di debug** (blocco `[fres-pick]` ×8, `[DIAG centro-vs-asse]`, `[ROBUST-DIAG]`, `[Synthesis Core]`, funzione console-only `syntesisDebugLabels`) e l'asset orfano `conn/AB-AR-00.stl` (zero riferimenti; `IPD.AB-SR-01-ZI.stl` non più fetchato dall'8.69.3 ma la History lo cita — conservato). **8.76.0 (B3) — markup unico per i 3 alberi interni di v3b** (spec = Analizza): pillole `.tree-group-header` FILE / SCANSIONI + MARKER anche in `sostRebuildTree`/`replaceRebuildTree` (in Replace la pillola MARKER è cliccabile col `.chevron` per il collapse, feature conservata); `.sost-caret` inline → `.chevron` (selettore CSS esteso alle pillole); righe senza flex inline (gap 6 della classe `.tree-node`); slider = `.tree-opacity-row`/`.tree-opacity-slider` NATIVI con `accent-color` (default `var(--blue)`, override per-oggetto: colore scansione reale, `tpl.color`, `var(--ghost)` Madre, colore figlio — il vecchio thumb custom webkit blu-fisso è stato rimosso, ora anche lo slider scan di Analizza è nativo); % label = `.tree-opacity-value`; token: `#0D1B2A`→`var(--dark)`, `#0A84FF`→`var(--blue)`, attivo `#0065B3`→`var(--blue)`, `dashed #333`→`var(--dark)` (×4 incl. Analizza), select figlio `#fff`→`var(--white)`.

Sorgenti primarie:
- `backend/main.py` — route che servono le pagine.
- `backend/static/syntesis-analyzer-v3b.html` — app analyzer (4 workflow interni).
- `backend/static/syntesis-icp-vedere.html`, `syntesis-dashboard-v1.html`, `syntesis-accedi.html`, `syntesis-gestione.html` — altre viste.

---

## Indice viste/route

| Vista | Route | File servito | main.py righe | Scopo sintetico |
|---|---|---|---|---|
| **Home** (splash) | `/` | `synthesis-home.html` | [145-154](../backend/main.py#L145) | Splash pubblica **dark**: bordo perimetrale animato + hero (logo + headline + immagine in card chiara) + 4 card workflow. **8.6.4**: logo dentro l'hero, top allineato all'immagine, layout più ampio e centrato. Sostituisce il vecchio redirect 302 a `/vedere` (fallback se il file manca). |
| **Vedere** (landing) | `/vedere` | `syntesis-icp-vedere.html` | [177-184](../backend/main.py#L177) | Viewer 3D multi-formato (STL/OBJ/PLY/XYZ/PCD/PTS) con strumenti di misura, forme, annotazioni. Home di default. NON è uno dei 4 workflow dell'analyzer. **8.78.0**: gated (`syn-gate.js`). **8.110.0**: auto-load da deep-link — `autoLoadFromURL` (Blocco 5r) apre `?file_id=` (Google Drive, via proxy) **e** `?url=<link>&file_name=` (URL esterno `https` `*.supabase.co`, `fetch` client-side → `loadFile`; usato da ac-ordini per l'STL paziente). **8.111.0**: `addLayer` apre ogni file con colore costante `VEDERE_OPEN_COLOR` (#FF8533) e le mesh in render-mode `both` (overlay reticolo creato al load); pointcloud restano `solid`. |
| **Analizzare** | `/analizzare` | `syntesis-analyzer-v3b.html` | [169-174](../backend/main.py#L169) | App di analisi di precisione (~3.87 MB monolite). 5 workflow interni — analizza, accoppia, misurare, sostituire, **replace (Replace-iT, 8.18.0)** — gestiti da `selectWorkflow`. |
| **Dashboard** | `/dashboard` | `syntesis-dashboard-v1.html` | [186-192](../backend/main.py#L186) | Area personale utente (mie analisi, profilo). **8.78.0**: gated (`syn-gate.js`). |
| **Accedi** | `/accedi` | `syntesis-accedi.html` | [194-201](../backend/main.py#L194) | Accesso a 3 stati: login, registrazione senza licenza (utente pending), pannello attesa con polling `/auth/me` + vista autorizzato. |
| **Gestione** | `/gestione` | `syntesis-gestione.html` | [203-210](../backend/main.py#L203) | Pannello admin: lista registrati, autorizza (genera chiave) / revoca. **8.16.0**: sezione "Librerie Replace-iT" (ingest/lista/verifica/attivazione librerie scanbody Exocad). **8.50.0**: sezioni "Archivio STL" (cartella unica per nome, lucchetto + codice, anteprima 3D) e "Crea libreria" (editor a righe madre/figlio + import CSV + template). **8.51.0**: pannello in **4 TAB** (`.admin-tabs` + `<section class=tabpane>` + `switchAdminTab`): Richieste di accesso / Librerie Replace-iT / Archivio STL / Crea libreria; **"Carica ZIP"** nella tab Crea libreria; ruolo a 3 stati (madre/figlio/**entrambi**); dettaglio libreria **editabile** (componenti modificabili/disattivabili/eliminabili). **8.67.0 — RBAC a 3 livelli**: ruoli super-admin (`role='admin'`) / **contribuente** (nuovo) / cliente (`role='user'` autorizzato). Gli endpoint `/admin/*` e `/admin/rit/*` passano da `require_admin` a **`require_contributor`** (admin O contribuente, ruolo riletto dal DB via `auth._fresh_role` — il JWT puo' essere stantio), TRANNE 2 super-admin-only: **`PATCH /admin/users/{id}/role`** (nuovo, assegna ruolo, `database.update_user_role` whitelist+anti-lockout) e `POST /admin/rit/lock-code` (codice-lucchetto). Guardia `_guard_not_superadmin` (un contribuente non agisce sul super-admin). UI: colonna **Ruolo** + **dropdown modifica-ruolo** visibile solo al super-admin (`MY_ROLE` via `/auth/me` → `setUserRole`/`onRoleChange`); 'Cancella cliente' = revoke (disattiva, reversibile). `syntesis-accedi.html classify()`: staff (admin+contribuente) autorizzati senza licenza. **8.109.0 — REDESIGN "Cockpit tecnico"**: i 4 TAB (`.admin-tabs`) diventano una **SIDEBAR** (`<aside class=sb>` con brand Synthesis ICP + nav `.sb-item[data-pane]` + chip Amministrazione), il contenuto va in `<div class=main>` con **topbar sottile**; `switchAdminTab` (2 occorrenze) ora seleziona `.sb-item` invece di `.admin-tabs .atab` (logica toggle `.active`+`tabpane` per `data-pane` invariata). Re-skin completo via `:root` superset: sidebar scura ink-blu, accent BLU Synthesis (no teal), **thead scuri**, **pill sature** (bosco/ambra/blu/indaco/mattone), KPI peso 700, `focus-visible`; ~6 literal colore JS allineati (chip file/conteggi/pallini lucchetto/miniatura). Colori-asse anteprima STL 3D (X rosso/Y verde/Z blu/ambra) **preservati**. `.admin-tabs`/`.atab` restano definiti in CSS (**dead code**, non più usati). Step 2 pendente: icone/mini-chart KPI, ordinamento colonne, empty-state. |

Note di completezza:
- I 6 file `.html` in `backend/static/` (accedi, analyzer-v3b, dashboard, gestione, icp-vedere, **synthesis-home**) sono **tutti** serviti dalle route sopra — nessun HTML orfano.
- Le `RedirectResponse` in `backend/main.py` sono i redirect del flusso OAuth Google Drive (`/auth/gdrive/connect-init` + `/auth/gdrive/connect` + `/auth/gdrive/callback`; **8.78.0 — audit C1**: connect consuma un codice one-time, niente JWT in query string), **non** viste HTML → fuori mappa.
- `auth.py` / `admin.py` non servono HTML (solo API JSON) — verificato (grep `FileResponse|HTMLResponse|.html` → vuoto).

### Workflow interni dell'analyzer `/analizzare`

Non sono route: sono stati interni di `syntesis-analyzer-v3b.html`, commutati da `selectWorkflow(wf)` ([4958](../backend/static/syntesis-analyzer-v3b.html#L4958)). `vedere` dentro `selectWorkflow` è solo un redirect a `/vedere` ([4961-4965]).

| Workflow | Ramo in `selectWorkflow` | `analysisMode` | Note |
|---|---|---|---|
| analizza | [5042](../backend/static/syntesis-analyzer-v3b.html#L5042) | `'misura'` | Default all'avvio (menu item `active`, [1283]). Analisi angolare MUA. |
| accoppia | [5064](../backend/static/syntesis-analyzer-v3b.html#L5064) | `'accoppia'` | Esporta/confronta accoppiamenti. |
| misurare | [5084](../backend/static/syntesis-analyzer-v3b.html#L5084) | `'misurare'` | Monta viewport ICP dedicato (2 STL). |
| sostituire | [5110](../backend/static/syntesis-analyzer-v3b.html#L5110) | `'sostituire'` | Posizionamento marker su scansione di partenza. |
| **replace** | [~5139](../backend/static/syntesis-analyzer-v3b.html#L5139) | `'replace'` | **Replace-iT (8.18.0, slice 2b-1)**: workflow NUOVO SEPARATO (Sostituisci resta bit-identico). `WORKFLOWS['replace']` [~4699]. Mostra `#panelReplace` [~2007], nasconde tutti gli altri pannelli/toolbar. **8.92.0 (F6e)**: le 92 fn del dominio (`replace*`/`_replace*`: cascata librerie, seeding 3-punti, allineamento, raffina client/server, export, taglio, anteprima 3D) vivono in `wf/replace.js`; lo STATO replace + banner §REPLACE-IT/§REPLACE-CUT-SCAN + il listener pointerdown (`replacePickDown*`) restano nel monolite; formula NDC preservata verbatim. Vedi sezione dedicata sotto. |

---

## Classi di visibilità (toolbar analyzer)

Classi CSS che `selectWorkflow` usa per mostrare/nascondere i pulsanti di toolbar per-workflow. Tutte in `syntesis-analyzer-v3b.html`.

| Classe | # occorrenze | querySelector | analizza | accoppia | misurare | sostituire | Note |
|---|---|---|---|---|---|---|---|
| `.analisi-only` | 11 | [5006](../backend/static/syntesis-analyzer-v3b.html#L5006) | mostra `''` [5053] | mostra `''` [5075] | nascondi `none` [5100] | nascondi `none` [5125] | Bottoni Analizza/Accoppia (es. "+ Posiziona", "Raffina"). |
| `.misurare-only` | 2 | [5007](../backend/static/syntesis-analyzer-v3b.html#L5007) | nascondi `none` [5054] | nascondi `none` [5076] | mostra `''` [5101] | nascondi `none` [5126] | |
| `.sostituire-only` | 4 | [5127](../backend/static/syntesis-analyzer-v3b.html#L5127) inline + gestione centralizzata a fine `selectWorkflow` | nascondi `none` | nascondi `none` | nascondi `none` | mostra `''` | **Corretto in 8.4.6**: gestione centralizzata in `selectWorkflow` (`querySelectorAll('.sostituire-only')` + display per `wf === 'sostituire'`), simmetrica al fix `#panelScanbodyType` 8.4.5. Visibili solo in sostituire, nascosti altrove. |
| `.sostituire-only` colonna replace → nascondi `none` | — | — | — | — | — | — | (per la riga `.sostituire-only` sopra: in `replace` resta `none`, gestita dal blocco centralizzato `wf === 'sostituire'`). |
| `.replace-only` | 8 (3 in 8.18-8.20; +3 in 8.21.0; +1 in 8.59.0; +1 in 8.62.0) | [~5178](../backend/static/syntesis-analyzer-v3b.html#L5178) gestione centralizzata a fine `selectWorkflow` | nascondi `none` | nascondi `none` | nascondi `none` | nascondi `none` (replace → mostra `''`) | **8.18.0**: gemello di `.sostituire-only` (`querySelectorAll('.replace-only')` + display per `wf === 'replace'`). Marca i bottoni di `#panelReplace`: "+ Posiziona (3 punti)" (`#replaceBtnPlace`), "Annulla punto" (`#replaceSeedUndo`), "Ricomincia" (`#replaceSeedReset`); **8.21.0**: "Avanti / nuovo" (`#replaceBtnNext`), "Concludi · Esporta" (`#replaceBtnFinish`, **8.46.0 funzionale** → `replaceExportSTL`); **8.22.0**: "Raffina" (`#replaceBtnRefine`) → `replaceRefineAll` (ICP per-marker); **8.59.0**: "Raffina+" (`#replaceBtnRefineSrv`, verde) → `replaceRefineServer` (ICP point-to-plane SERVER full-res, opzione parallela). **8.62.0**: Raffina e Raffina+ ora calcolano il **righello A/B comune** (`_replaceRecordAB`/`_replaceEvalFit` RMSD point-to-point µm + delta-clocking vs seed `_replaceTwistAngleDeg`) → riga-verdetto; NUOVO "↺ Seed" (`#replaceBtnResetSeed`) → `replaceResetToSeed` (riporta l'impianto attivo alla posa seed 3-punti per l'A/B indipendente). Abilitazione gestita in `replaceRebuildPlacedList` come Raffina/Raffina+. **8.72.0**: Raffina+ e ↺ Seed anche `.expert-only` (area Expert); righello A/B calcolato/mostrato solo in expert. |
| `.icon-only` | 26 | — | n/a | n/a | n/a | n/a | **Non** è gating per-workflow: styling per bottoni a sola icona. Citata per completezza. |
| `.expert-only` / `.expert-hide` | 9+1 CSS / 2+1 CSS | CSS `body:not(.expert-mode) .expert-only{display:none !important}` + `body.expert-mode .expert-hide{display:none !important}` (coda del `<style>` principale, ~[1145]) | n/a (trasversale ai workflow) | — | — | — | **8.72.0 — AREA EXPERT**: elementi diagnostici/beta visibili solo con `body.expert-mode`. Sblocco: pallino `#synLogDot` (in basso a sx) → `prompt()` password → se = `SYN_EXPERT_PW_HASH` (SHA-256, ~[2472]) → `synExpertToggle`/`synExpertApply` (~[2496-2515]): classe body + `window.SYN.expert` + `localStorage['syntesis_expert_mode']` (ripristino in `synLogInit`; pallino blu `#0065B3` = attiva; la password log `SYN_LOG_PW_HASH` continua a scaricare il log). Gated `.expert-only`: Raffina+ `#replaceBtnRefineSrv`, ↺ Seed `#replaceBtnResetSeed`, span RMSD albero Replace (×2), RMSD lista MUA + albero Analizza (con `.expert-hide` = 'Accoppiato'/'accoppiato' per l'utente medio), cella `#misSumRmsd` (Misurare), bottone CSV `sostDownloadDiagLog` (Sostituire), tab Impostazioni→Algoritmo `#settingsTabAlgoBtn`, **8.100.0** pulsante `#synLogBtn` "⤓ log". Gated via JS (`window.SYN.expert`): righello A/B `_replaceRecordAB` (early-return, salta anche `_replaceEvalFit`), stringhe RMSD negli status (ICP Misurare, posa/conferma Replace), RMSD nella label lista impianti (textContent). Occultamento UI, NON sicurezza (gating sensibile = server-side, §5 CLAUDE.md). **8.100.0 — SESSION LOG "stella polare"**: `synLog(cat,msg,data)` ora redige a monte via `_synRedact` (chiavi pass/pwd/token/jwt/secret/authoriz/bearer/cookie/api_key/credential/hash/session + forma JWT/bearer) + `_synRedactUrl` (query param sensibili); `_synInstallHooks` (1 volta in `synLogInit`) installa wrapper `window.fetch` (net: metodo/path-redatto/stato/ms, MAI header/body), delega `click`/`change` document-level (elemento, MAI valori campi; file names sì, input value NO), cattura `console.error/warn`; pulsante `#synLogBtn` `.expert-only` → `synLogDownloadAll` scarica FRONT (`synLogText`) + BACKEND (`GET /api/logs`, token solo in header) in un `.txt`. BACKEND (`main.py`): buffer RAM `_SYN_LOG_BUF=deque(3000)`, `_syn_back_redact` (KV-contiene per env var + URL-cred + JWT/bearer), `_SynBufHandler` WARNING+ su root logger, middleware `_syn_request_logger` ([REQ] per richiesta, salta /static/ e /api/logs), endpoint `GET /api/logs` (`require_authorized`). |

### Workflow Replace-iT (`replace`, 8.20.0 — slice 2b-1.2: seeding 3 punti + anteprima interattiva)

Quinto workflow, **NUOVO e SEPARATO**: clona i mattoni di Sostituire — che resta **bit-identico** (nessun `if(replace)` nelle sue funzioni) — e riusa i **mattoni puri** `parseSTL` (scan), `sostParseSTLToGeometry` (marker), `findScanbodyCenter` (solo l'**asse**). Consuma la superficie 2a `/api/rit/*` (`require_authorized`, Bearer da `localStorage['syntesis_token']`). **NON** include match/Raffina (slice 2b-2).

| Elemento | id / selettore | Evento | Funzione | Effetto | Riga |
|---|---|---|---|---|---|
| Voce menu | `[data-wf="replace"]` | click | `selectWorkflow('replace')` | attiva il workflow | [~1304] markup |
| Pannello | `#panelReplace` | — | — | scan + **cascata Marca→Modello→Ø** (8.52.0) + Madre/Figlio + lista marker | [~2007] |
| File scan | `#replaceInputScan` / `#replaceSlotScan` | change | `replaceOnScanPicked` → `replaceLoadScanToScene` | carica STL scansione in `replaceMesh` (≠ `sostMesh`) | [35884](../backend/static/syntesis-analyzer-v3b.html#L35884)/[35918](../backend/static/syntesis-analyzer-v3b.html#L35918) |
| **Cascata libreria (8.52.0)** | `#replaceMarcaSelect` / `#replaceModelloSelect` / `#replaceDiamSelect` | change | `replaceOnMarcaChange` / `replaceOnModelloChange` / `replaceOnDiamChange` (+ `replaceBuildCascade` all'init, `replaceCascadeReset`) | 3 tendine dipendenti raggruppate per `marca`/`modello`/`diametro` da `replaceLibs`; il Ø atterra sulla libreria → `replaceLoadLibrary(id)` → `GET /api/rit/libraries/{id}` → popola Madre/Figlio. Sostituisce il vecchio `#replaceLibSelect`/`replaceOnLibChange` (rimossi) | markup [~2017]; `replaceBuildCascade` [35736] / `replaceLoadLibrary` [35788] |
| Dropdown **Madre** (sorgente) | `#replaceSourceTypeSelect` | change | `replaceOnSourceTypeChange` | type **filtrato per ruolo** madre\|entrambi (8.52.0; fallback: tutti se la libreria non ha ruoli, es. LITE) → anteprima marker + 3 punti | markup [~2030]; `replacePopulateTypeOptions(...,'madre')` |
| Dropdown **Figlio** (sostituto) | `#replaceTypeSelect` | change | `replaceOnTypeChange` | type **filtrato per ruolo** figlio\|entrambi (8.52.0; fallback: tutti); azzera il seme (`replaceSeedClear`) | markup [~2034]; `replacePopulateTypeOptions(...,'figlio')` |
| Finestra guida accoppiamento | `#replacePreviewBox` / `#replacePreviewCanvas` | mousedown (canvas) + window mousemove/mouseup (orbit/click) | `replacePreviewInit/Render/Dispose` → `replacePreviewPickAt`; `replaceGuideRender` | **8.27.0 (2b-3b)**: finestra **fluttuante** `position:fixed` top-level in basso a sinistra (non più 300px nel pannello): secondo `THREE.WebGLRenderer` (**rotazione libera trackball** a quaternioni dal 8.29.0 — `replacePreviewCam.q`, niente clamp polare; raccoglie i 3 punti del seme, dot 1 rosso/2 verde/3 blu, frame locale via `worldToLocal`) + header **Impianto #N** (`#replaceGuideTitle`) + **badge 3 passi** (`#replaceGuideSteps`) + **testo-passo** dinamico (`#replaceGuideStep`) + counter/Annulla/Ricomincia/Conferma (spostati qui dal pannello, stessi id). **8.36.1 (fix sovrapposizione ↔ Albero scena)**: visibilità gestita per FASE in `replaceSeedUpdateUI` (gated `analysisMode==='replace'`) — mostrata SOLO durante il piazzamento attivo (`pickMarker`/`pickScan`/`pendingConfirm`), nascosta quando `idle` → l'Albero scena `#layersPanel` torna pienamente cliccabile (prima la guida z25 copriva l'albero z8). Durante il piazzamento `#layersPanel` è limitato al top (`max-height: max(120px, calc(100% - 28px - 42vh - 190px))`, riserva in basso lo spazio della guida) → reset altezza piena a `idle` e all'uscita da replace (`selectWorkflow`). **8.41.0 (design system)**: colore **Madre** da verde `#2DBE8B` (conflitto col verde clinico) a **grigio-blu ghost** `#8090A8` (token `--ghost`); box **"Flusso"** del `#panelReplace` da oliva off-palette a callout tokenizzato (`var(--pearl)`/`var(--border)`/`var(--dark)`); verde `#0D9E6E` di `#replaceBtnConfirm`/`#replaceBtnPlace`/badge passo → `var(--green)`. **8.44.0**: tema della finestra guida portato a quello dell'**Albero Scena** `#layersPanel` (chiaro: bianco translucido `rgba(255,255,255,0.82)`, `var(--border)`, blur, testi `var(--dark)`/`var(--gray)`; bottoni chiari; canvas 3D resta scuro) — supera la scelta "scuro" di 8.41.0 su richiesta utente; **rimossi i 3 testi esplicativi lunghi dal `#panelReplace`** (intro/Flusso/`#replaceGuide`). | [1474](../backend/static/syntesis-analyzer-v3b.html#L1474)/[1481](../backend/static/syntesis-analyzer-v3b.html#L1481) markup (top-level dopo `#cutViewOverlay`); `replacePreviewInit`/`replaceGuideRender`/`replaceSeedUpdateUI` |
| Vista Madre + Figlio + origine (pending) | `#replaceViewRow` → `#replaceViewSrc`/`#replaceViewSub`/`#replaceViewOrigin` (finestra guida) | change | `replaceSetPendingMeshVis` / `replaceTogglePendingOrigin` | **8.33.0** (rev. di 8.32.0): a fase `posed`, 3 checkbox — **Madre** (sorgente, grigio-blu ghost `--ghost` dal 8.41.0) + **Figlio** (sostituto) + **origine x0/y0/z0** — con visibilità INDIPENDENTE (`replacePending.showSrc`/`showSub`/`showOrigin` → `_replaceApplyView`); default ENTRAMBI on + origine on. Sostituisce il toggle esclusivo Sorgente↔Sostituto. gating/sync in `replaceSeedUpdateUI`. | markup riga `#replaceViewRow`; `replaceSetPendingMeshVis`/`replaceTogglePendingOrigin`/`_replaceApplyView` |
| + Nuovo impianto | `#replaceBtnPlace` `.replace-only` | click | **8.36.0** `replaceStartNewImplant` → `replaceStartThreePoint` (fase `pickMarker`) → `replacePreviewPickAt` (3 marker) → `replaceOnViewportClick` (fase `pickScan`, 3 gemelli) → `replacePlaceFromSeed` | **ingresso DIRETTO ai 3 punti** (salta il click-scanbody/pickSource + il pulsante ▶ Allinea); valida scansione+type+anteprima poi avvia il seeding 3+3. `replaceStartPlacement` (vecchio source-first) **rimosso in 8.40.0**. `replaceNextScanbody` ("Avanti/nuovo") → `replaceStartNewImplant`. | [markup `#replaceBtnPlace`]; `replaceStartNewImplant`/`replaceStartThreePoint` |
| Contatore seme | `#replaceSeedCounter` | — | `replaceSeedUpdateUI` | stato avanzamento seme (punti raccolti / fase) | [1490](../backend/static/syntesis-analyzer-v3b.html#L1490) markup; [35616](../backend/static/syntesis-analyzer-v3b.html#L35616) |
| Annulla punto | `#replaceSeedUndo` `.replace-only` | click | `replaceSeedUndo` | rimuove l'ultimo punto del seme (anteprima o scansione) | [1491](../backend/static/syntesis-analyzer-v3b.html#L1491) markup; [35591](../backend/static/syntesis-analyzer-v3b.html#L35591) |
| Ricomincia | `#replaceSeedReset` `.replace-only` | click | `replaceSeedClear` | azzera il seme e i dot, torna a fase `idle` | [1492](../backend/static/syntesis-analyzer-v3b.html#L1492) markup; [35570](../backend/static/syntesis-analyzer-v3b.html#L35570) |
| **(per riga)** Elimina scanbody | `✕` in `#replacePlacedItems` (injected) | click | `replaceDeletePlaced(id)` | **8.21.0**: rimuove dalla scena marker (group + 3 dot, dispose) e dal record `replacePlaced`; copre "cancellare/tornare indietro" su qualsiasi riga | markup dinamico; `replaceRebuildPlacedList` / `replaceDeletePlaced` |
| Flusso: Raffina / Raffina+ / Avanti·nuovo / Concludi | `#replaceFlow` → `#replaceBtnRefine` / `#replaceBtnRefineSrv` / `#replaceBtnNext` / `#replaceBtnFinish` `.replace-only` | click | `replaceRefineAll` (Raffina) / `replaceRefineServer` (Raffina+) / `replaceNextScanbody` (Avanti) | mostrato con ≥1 marker. **Raffina+ (8.59.0 — opzione PARALLELA, richiesta utente "togliere incertezza alla posa della madre, opzione da provare in live")**: `#replaceBtnRefineSrv` (verde `#0E8C6A`, accanto a Raffina, abilitato/disabilitato insieme a `#replaceBtnRefine` in `replaceRebuildPlacedList`) → `replaceRefineServer()`: estrae la MADRE (sorgente `p.srcGeo` in MONDO) a **piena risoluzione** + crop cilindrico scansione (stessa logica di `_replaceDoRefine`, che NON viene toccata; cap `REPLACE_SRV_MAX_SRC`=8000 / `REPLACE_SRV_MAX_TGT`=20000), **POST `/api/rit/refine-icp`** (Bearer `syntesis_token`) → ICP **point-to-plane full-res** server (`icp_engine.refine_point_to_plane`, cKDTree, solve 6×6 + fallback Kabsch, trimming mediana×2.5 + rifiuto-normali, peso 5× cap) → applica lo **stesso delta rigido 4×4** sul group (riuso del blocco di `_replaceDoRefine`: `decompose` + `p.position`/`p.axisDir`) → `replaceRebuildScanGeometry` + `replaceRebuildPlacedList`; status con **RMSD/coppie/iter/ms** per confronto **A/B** col client. Validato offline ~1µm (vs ~5-9µm client) sui 3 scanbody reali. Self-contained, additivo: il path client (`_replaceDoRefine`/`replaceRefineAll`) resta intatto. **Raffina (8.22.0 → AUTO-LOOP 8.25.0)**: ICP per-marker `_replaceDoRefine` (campiona marker nel mondo + pesi facce-piatte ×5 → crop cilindrico scan → ICP pesato+outlier Tukey+convergenza+idempotenza → delta rigido al group; riuso `kabsch`); abilitato con ≥1 marker; RMSD mostrato in lista. **8.25.0 (2b-2.1)**: `replaceRefineAll` ripete `_replaceDoRefine` su tutti i marker **round-dopo-round** (ogni round ri-ritaglia attorno alla posa corrente = coordinate-descent) finché nessuno si muove oltre soglia (centro <1µm E asse <~0.011°) o cap 12 round; loop **asincrono** `setTimeout(0)` tra i round (UI reattiva, progresso live), `#replaceBtnRefine` disabilitato durante il run, stato finale "a convergenza in N round — RMSD medio". **8.56.0** (richiesta utente: "Raffina solo su quello che stiamo posizionando, non tutti"): `replaceRefineAll(onDone, targetNum)` raffina SOLO il marker `targetNum` se passato (altrimenti tutti, legacy); il pulsante Raffina → `replaceRefineCurrent()` risolve il marker **attivo** (`replaceActiveNum`, default = ultimo piazzato via `_replaceRefineTargetNum`; selezionabile cliccando il nome nell'albero, vedi riga Albero); il **✓ Conferma** raffina SOLO l'impianto appena confermato; `doRound` ri-filtra i `targets` ad ogni round (scarta i marker eliminati durante la raffina, come il flusso swap). `_replaceDoRefine` INVARIATO. **Avanti/nuovo**: azzera type+seme+anteprima per il prossimo scanbody. **8.47.0**: il **✓ Conferma** (`replaceConfirmSeed`) ora **chain**a Conferma → `replaceRefineAll` (ICP auto-loop) → a convergenza **attiva `cutScan`** sull'impianto (callback `onDone`) → `replaceRebuildScanGeometry`+albero (auto-taglio scansione, raggio adattivo). **8.57.0** (richiesta utente con screenshot, auto-config albero dopo Raffina): nello stesso `onDone` la **MADRE viene nascosta** (`_m.showSrc=false`+`_replaceApplyView`) → vista pulita del risultato (Figlio + taglio; la Madre = scanbody di riferimento resta ri-attivabile dall'albero); prima (8.33.0) restava visibile translucida. **Concludi · Esporta** (`#replaceBtnFinish`, **8.46.0** — era scaffolding disabilitato): `replaceExportSTL` apre il dialog nome `#replaceExportDialog` (gemello `#sostExportDialog`); `confirmReplaceExport`→`_replaceDoExportSTL` costruisce un unico STL binario dei **sostituti** (figli `p.meshSub`) di tutti gli impianti, vertici in **mondo** via `group.matrixWorld`, e lo scarica (`writeBinarySTL`). La madre NON e' esportata. Guardie su `replacePlaced` vuoto/geometria assente. **8.72.0**: `#replaceBtnRefineSrv`/`#replaceBtnResetSeed` = `.expert-only`; `_replaceRecordAB` no-op senza area Expert. | [~2055-2060](../backend/static/syntesis-analyzer-v3b.html#L2055) markup; `_replaceDoRefine`/`replaceRefineAll` |
| Lista marker | `#replacePlacedList` | — | `replaceRebuildPlacedList` | elenco multi-marker piazzati (pannello, per il flusso). **8.53.0**: voce = `#N Marca Modello Ømm · RMSD` (`_replaceLibId`; fallback `typeLabel`) | [36313](../backend/static/syntesis-analyzer-v3b.html#L36313) |
| Albero scena | `#layerTree`/`#layersPanel` (sx) | checkbox/color/range/chevron/✕ | `replaceRebuildTree` (8.26.0 → **stile Analizza 8.28.0**) | **2b-3a**: scansione (visibilità/colore/opacità) + per-marker (visibilità/colore/RMSD/elimina), gemello `sostRebuildTree`, target colore `replacescan`/`replace:`+num. **2b-3d (8.28.0, ricalcato da Analizza)**: gruppo **Marker (N) collassabile** (chevron, `replaceToggleMarkersCollapse`/`replaceTreeUI`) + per-marker **sub-riga "Taglia scansione"** (`replaceToggleMarkerCut`) + massiva **"Taglia scansione (tutti)"** (`replaceToggleAllCuts`, indeterminate). **Motore taglio** `replaceRebuildScanGeometry` (clone di `rebuildScanMeshGeometry`): ricostruisce `replaceMesh.geometry` dalla **originale** escludendo i triangoli dentro i cilindri dei marker con `p.cutScan` (**8.45.0**: raggio ADATTIVO per-marker `replaceEstimateMarkerRadius` = 90° perc. estensione radiale del figlio + 0.3mm, cap al vecchio 3mm → taglia il footprint dello scanbody, non un'ampia fascia x/y; ±30mm su `axisDir`) → buco per vedere lo scanbody seduto; rebuild su toggle/delete/fine-Raffina. **8.54.0** (feedback utente "il taglio deve togliere tutto attorno al file… guidato dal file madre"): raggio dal **MADRE** (`p.meshSrc`, ciò che occupa davvero lo spazio nella scansione) non più dal figlio; + **slider OFFSET per-marker** sulla riga "Taglia scansione" quando attiva (`replaceSetMarkerCutOffset`, 0-5mm step 0.1, label +X.Xmm, sommato DOPO il cap 3mm → può superarlo per "togliere tutto attorno"; **default 0.5mm** (8.57.0, scelta utente; era 0): l'oggetto impianto nasce con `cutOffset:0.5`; ricostruzione **debounced 120ms** — review perf; timer azzerato su toggle/delete). **8.55.0** (feedback utente: "il taglia scansione dipende dalla forma del figlio mentre dovrebbe dipendere dalla forma della madre"): il taglio non è più un **cerchio capato a 3mm** ma segue la **SILHOUETTE per-angolo del madre** (`_replaceMadreProfile`: 48 settori attorno all'asse, 85° percentile del raggio dei vertici del madre per settore in MONDO, ignora flare del cap + outlier, settori vuoti = mediana, guardia 8mm); `replaceRebuildScanGeometry` usa la soglia per-settore `prof[settore]+offset` (no `sqrt`, `atan2` per il settore) invece del raggio² unico → il buco segue la forma reale dello scanbody; fallback al cerchio (`replaceEstimateMarkerRadius`) per marker single-mesh / profilo assente. **8.59.6** (feedback utente: il taglio prendeva tessuti/denti adiacenti): non più tubo passante ±30mm ma limitato all'**estensione assiale reale del madre** — `_replaceMadreProfile` traccia `axMin/axMax`; il cilindro porta la banda `[axLo,axHi]=[axMin-off,axMax+off]` clampata a ±30mm; loop di taglio e `_replaceRemoveCutIslands` usano la banda. **8.59.7** (feedback utente "taglia col file intero, non il ridotto dell'anteprima"): profilo + estensione assiale dal **CAD madre INTERO** (`p.srcGeoFull` = geometria piena dalla cache, helper `_replaceCutSourceGeo`), non la versione accorciata dallo slider taglio dell'anteprima (che resta solo per il fit `p.srcGeo`, design 8.43.0). **8.59.9** (feedback utente: due strisce parallele di over-cut nella mucosa): **tetto robusto al profilo** `REPLACE_PROFILE_CAP_K=1.2` — nessun settore supera `K ×` la mediana (≈ raggio del corpo cilindrico) → rigetta le feature non-round del madre (square engaging sub-gengivale/scan-flag) che nella scansione paziente sono solo mucosa. **8.59.4** (feedback utente: "il taglio lascia frammenti di STL vaganti, piccole isole di triangoli staccate da tutto"): dopo aver costruito `kept`, `_replaceRemoveCutIslands(kept, cyls)` fa **connected-components** (saldatura vertici su griglia 1µm + union-find) e rimuove le componenti **piccole** (size <5% del totale), **non la più grande** (= corpo scansione), che cadono in **maggioranza** (>60% triangoli) nell'intorno del cilindro di taglio (raggio `profMax`+offset+2mm, asse ±`halfH`+2mm); preserva corpo principale, "metà" grandi da through-cut e anatomia lontana. Gira nel rebuild già **debounced 120ms** (no lag sul drag). **Disaccoppiamento**: seed (`replaceEstimateCylinderAxis`/`findScanbodyCenter`) e Raffina (`_replaceDoRefine` crop) usano `replaceOriginalGeo` (piena) → il taglio non rompe fit/crop. **8.33.0** (rev. di 8.32.0): i marker **auto-posa** diventano **"#N impianto"** (header = on/off gruppo + RMSD + elimina) con **due sotto-voci indipendenti** — **"Madre · `<srcTypeLabel>`"** (visibilità `replaceSetMarkerMeshVis('src')` + colore `setSceneObjectColor('replacesrc:'+num)` → `meshSrc`) e **"Figlio · `<typeLabel>`"** (visibilità `replaceSetMarkerMeshVis('sub')` + colore `'replace:'+num` → `meshSub`) — + sotto-voce **origine x0/y0/z0** (`replaceToggleMarkerOrigin`). Madre e figlio compaiono entrambi nell'albero, visibilità indipendente (`_replaceApplyView`). **8.38.0**: la sotto-voce **Figlio** è un **DROPDOWN** dei type della libreria (`p.libTypes`, snapshot al piazzamento) → **`replaceSwapFiglio(num, ord)`** richiama un altro IPD della stessa madre (swap della sola `meshSub.geometry` alla stessa posa, niente ri-accoppiamento; vecchia geo in cache, non disposta). I marker "Allinea a 3 punti" (1 sola mesh) restano sulla riga classica (gate `if(p.meshSrc)`). **8.39.0**: (a) **trasparenza per-oggetto** — sotto Madre e sotto Figlio uno **slider** `range` (`replaceSetMarkerOpacity(num,'src'|'sub',pct)` → `mesh.material.opacity`/`transparent`; label % `#replaceOpLbl_{src|sub}_{num}`; accent Madre `#8090a8` (ghost, 8.41.0), Figlio = colore figlio), gemelli dello slider scansione (`replaceSetScanOpacity`); (b) **modalità render globale** (solid/wireframe/both della barra `#vmBar`) ora raggiunge le mesh replace — `replaceRebuildTree` chiama in coda `replaceApplyRenderMode()` (scansione + madre/figlio + anteprima pending) e `applyRenderModeToScene` lo invoca a sua volta. **8.49.0** (item audit minore, multi-impianto): (a) **focus camera** — **doppio-clic sul nome** dell'impianto (`ondblclick="replaceFocusImplant(num)"` + `cursor:pointer` + title) inquadra il **figlio** (`p.meshSub`, fallback `p.group`): bbox in mondo `THREE.Box3().setFromObject`, `controls.target` sul centro + camera avvicinata mantenendo la direzione di vista corrente (`dist=rad/tan(fov/2)*1.7`), animazione ease-out ~280ms (lerp target+pos, pattern di `installAltClickPivot`); (b) **nome dente** — pulsante **✎** (`replaceRenameImplant(num)`) apre un `prompt()` per il numero dente FDI (es. "26") → `p.toothLabel` (vuoto = "#N impianto"), input ≤6 char **escapato** via `_escHtml`; etichetta "**#N · 26**". Su **entrambi** i rami (madre+figlio e single-mesh legacy). **8.53.0**: in assenza di nome-dente, il nodo impianto mostra l'**identità libreria** `#N Marca Modello Ømm` (`_replaceLibId`) invece del generico "#N impianto" (fallback al `typeLabel` se mancano marca/modello/diametro); su entrambi i rami. **8.56.0**: **clic sul nome** dell'impianto lo rende **attivo** per la Raffina (`replaceSetActiveImplant(num)` → highlight `▸ ` + colore `#0065B3` sulla riga; valida che il num esista; doppio-clic resta = focus camera); l'attivo (default = ultimo piazzato) è il target di `replaceRefineCurrent`/`_replaceRefineTargetNum`; l'eliminazione dell'attivo azzera `replaceActiveNum` (torna all'ultimo via resolver). NB: non persistito (saveCase non salva replace). **8.72.0**: lo span RMSD delle righe albero è `.expert-only`. | `replaceRebuildTree` / `replaceRebuildScanGeometry` / `replaceToggleMarkerCut` / `replaceToggleAllCuts` / `replaceToggleMarkersCollapse` / `replaceSetMarkerMeshVis` / `replaceToggleMarkerOrigin` / `replaceSwapFiglio` / `replaceSetMarkerOpacity` / `replaceApplyRenderMode` / `replaceFocusImplant` / `replaceRenameImplant` / `replaceSetActiveImplant` |
| Label 2D | `.replace-label` (`.divergence-label`) | loop `animate` | `replaceEnsureLabelElements` / `replaceUpdateLabels` | **8.53.0**: etichetta = **identità libreria** `#N Marca Modello Ømm` (`_replaceLibId`, da `p.marca/modello/diametro` congelati alla posa); **grafica/posizione come i MUA in Analizza** — `replaceUpdateLabels` clone di `updateDivergenceLabels` (pillola `.divergence-label` colore impianto `p.color`, **linea guida SVG** su `#labelLines` + compensazione `body.zoom`); rimosso lo sfondo scuro forzato `#0D1B2A`. **8.53.1**: offset in SCHERMO (`OFF_PX=54` px fissi dal marker, lungo la direzione dell'asse PROIETTATA, orientata verso l'alto; degenere → verticale) invece di 3D asse×10mm — **robusto allo scorcio** (i 10mm 3D collassavano sul marker in vista quasi-occlusale → distacco/linea invisibili). **8.59.8**: ancora al **CAP del FIGLIO** (`meshSub`, estremo più lontano dalla connessione via `Box3`) e lift **verso l'alto del figlio** (asse verso il cap, fallback alto-schermo allo scorcio), non più dalla connessione `p.position` (bassa/coperta dalla gengiva) → la linea parte dal sostituto e la pillola sale sopra senza coprirlo. **8.60.0** (design system pastello, commit 1/2): i colori di severità delle etichette di deviazione (`.divergence-label.good/.warn/.risk/.bad`, classi da `classifyDivergence` 15/25/45) sono stati **ri-puntati dai token UI** `--green/--gold/--red`(+`#F97316` hardcoded) **a token clinici dedicati** `--clin-good/--clin-warn/--clin-risk/--clin-bad` = palette **d3 canonica** (`#639922/#D97706/#F97316/#EF4444`): **disaccoppiati** dalla UI (il ripunto pastello del chrome nel commit 2 non li tocca) e **unificati** alla palette clinica (scelta utente). Prima (≤8.52.0): solo numeri `#N` proiettati | loop `animate` |

**⚑ FLUSSO ATTIVO (8.36.0–8.40.0): UNICO a 3 PUNTI, INGRESSO DIRETTO.** L'auto-ICP è **rimossa** (8.35.0 disattivata, scelta utente; **dead code rimosso fisicamente in 8.40.0**). **8.36.0 — flusso diretto + 4 migliorie**: **+ Nuovo impianto** (`replaceStartNewImplant`) → **SUBITO i 3 punti** (salta click-scanbody + ▶ Allinea) → 3 punti sul **marker** (anteprima) + 3 **gemelli** sulla scansione (`replacePlaceFromSeed`) → ✓ Conferma. (1) **guida** rietichettata 1 Marker / 2 Scansione / 3 Conferma + testi per-punto (`replaceGuideRender`); (2) **correzione**: rifiuto gemelli troppo vicini (<0.6mm) + "Annulla punto"; (3) **Raffina ICP bounded** AUTO dopo i 3 punti (in `replacePlaceFromSeed`: ≤3 iter `_replaceDoRefine` per stringere la posa). **8.37.0**: gira in un **tick separato** (`setTimeout 0`) con **feedback** (status "Raffino…" + cursor wait, dipinti perché si cede un frame); **gate rafforzato** — accettata SOLO se `p.rmsd` valido E ≤0.15mm E drift ≤0.3mm E rot ≤3° (bound stretti), altrimenti torna alla posa 3-punti. **8.37.0 — robustezza**: dropdown libreria/type **disabilitati durante il piazzamento** (proteggono il seme); **Ctrl+Z** workflow-aware in replace; errori fetch `/api/rit/*` leggibili (`_replaceFetchErrMsg`: 401/403→login, 404, rete). `replacePlaceFromSeed` costruisce MADRE (sorgente, `meshSrc` grigio-blu ghost dal 8.41.0, children[1]) + FIGLIO (sostituto, `meshSub`, children[0]) + terna origine (children[2]) → madre+figlio in scena (pending) e albero (confermati). `replaceAutoPlaceFromSource` + `_replaceEstimateCadRadius` + gate RMSD + `replaceStartPlacement` + ramo `pickSource` + fasi `chooseType`/`posed` + stati `sourceCenter`/`sourceAxis`/`sourceDot` + pulsanti `#replaceBtnAlign`/`#replaceBtn3pt` = **dead code RIMOSSO in 8.40.0** (net −241 righe; whitelist condivisi conservati: `findScanbodyCenter`, `replaceEstimateCylinderAxis`, `_replaceDoRefine`, `sostRobustCenter`, `replacePlacementMode`). _Sotto, la storia dell'auto-ICP 8.31–8.34 (rimossa):_

**[⚠ CODICE RIMOSSO in 8.40.0 — descrizione storica del binario auto-ICP, conservata per il razionale]** **Piazzamento — SORGENTE→SOSTITUTO (8.31.0, 2b-3f, come Sostituisci; trigger esplicito 8.31.2):** si scelgono **due type** della stessa libreria — **SORGENTE** (`#replaceSourceTypeSelect`, geometria del brand scansionato) + **SOSTITUTO** (`#replaceTypeSelect`, IPD da piazzare). **"+ Nuovo impianto"** → `pickSource` → **1 click** sullo scanbody (`findScanbodyCenter` centro + `replaceEstimateCylinderAxis` asse robusto, sfera gialla) → con scanbody + entrambi i type compare il pulsante **▶ Allinea (ICP)** (`#replaceBtnAlign`, finestra guida; gating in `replaceSeedUpdateUI`: solo fase `chooseType` con `sourceCenter`/`sourceAxis` + `replaceSourceType` + `replaceSubstType`). **8.31.2 — UNICO trigger esplicito:** premendolo parte **`replaceAutoPlaceFromSource`**; `replaceMaybeAutoPlace` **non auto-lancia più** l'ICP (solo refresh UI → niente freeze a sorpresa al cambio menu; fix del dead-end "② Marker" senza modo di avanzare). `replaceAutoPlaceFromSource`: **8.32.0** — il group contiene **DUE mesh** nello stesso frame, **SOSTITUTO** (`geoSub`, children[0] = marker finale; confirm/refine/dispose lavorano su children[0]) + **SORGENTE** (`geoSrc`, children[1] = ispezione fit, verde) + **terna ORIGINE** x0/y0/z0 (children[2], `_replaceMakeOriginAxes` all'origine locale 0,0,0 = origine CAD condivisa). **8.33.0**: MADRE e FIGLIO hanno **visibilità INDIPENDENTE** (`p.showSrc`/`p.showSub`/`p.showOrigin` + `_replaceApplyView`) → **entrambi visibili insieme** di default (madre verde translucida, opacity 0.5, come overlay del fit sopra il figlio); toggle a checkbox in finestra guida (pending) e nell'albero (confermati). `p.srcGeo=geoSrc`; **ICP full-surface** (loop 8 iter via `_replaceDoRefine`, che campiona `p.srcGeo`=sorgente e muove il group → allinea il **sorgente**, il sostituto eredita) → allinea il **sorgente** alla scansione a **6-DOF**; **8.34.0 — ricentraggio robusto del seed** (fix precisione: l'auto accoppiava male, CAD flottante RMSD ~0.65mm): prima del multi-start, `replaceAutoPlaceFromSource` deriva il raggio dal CAD sorgente (`_replaceEstimateCadRadius`) e ricentra il seed con **`sostRobustCenter`** (centro full-surface click-invariante ~µm, raggio libero, gate copertura 140° + fail-soft) → tutti gli 8 roll partono da un centro corretto (root cause era `findScanbodyCenter` al click con raggio default 1T3 sbagliato per il type). **multi-start del roll** (8.31.1: 8 roll iniziali, tiene il RMSD minore + avviso se ambiguo) per non bloccarsi su un roll sbagliato su corpi quasi-simmetrici; il sostituto, condividendo l'origine, eredita la posa. **8.34.0 — gate RMSD**: se `p.rmsd > REPLACE_AUTO_RMSD_GATE` (0.15mm) → **auto-fallback a `replaceStartThreePoint`** invece di mostrare una posa flottante. `srcMarkerSha` nel record → la **Raffina** ri-allinea sempre sul sorgente. **Niente 3 punti, niente roll arbitrario.** `replaceOnSourceTypeChange`/`replaceOnTypeChange`/`replaceMaybeAutoPlace`; stato `replaceSourceType`/`replaceSubstType`. Ramo `replaceStartThreePoint` ("Allinea a 3 punti", fase `posed`) resta come fallback (anche auto-fallback dal gate RMSD 8.34.0). **8.34.1**: `replacePlaceFromSeed` ora costruisce la **stessa struttura MADRE+FIGLIO+origine** dell'auto (Promise.all sorgente+sostituto → `meshSub` children[0] + `meshSrc` children[1] verde + `originAxes` children[2] + `showSrc`/`showSub`/`showOrigin`/`srcTypeLabel`); la posa 3-punti allinea il **sorgente** (i 3 click sono sul preview sorgente), il sostituto eredita via origine condivisa → madre+figlio compaiono in scena (pending) e nell'albero (dopo Conferma) anche col 3-punti. `#replaceViewRow` ora visibile anche a fase `pendingConfirm`. _Modello in [[replaceit-correct-model]]; sostituisce il wizard a 3-punti 8.30._ — _vecchia descrizione 8.30 sotto, storica:_ **WIZARD source-first (8.30.0, 2b-3e.2):** flusso per impianto guidato dalla finestra. **"+ Nuovo impianto"** (`replaceStartPlacement`) → fase **`pickSource`** ("① clicca lo SCANBODY da sostituire"); `replaceOnViewportClick` ramo `pickSource` = **1 click** → `findScanbodyCenter(replaceOriginalGeo)` per il **centro** + `replaceEstimateCylinderAxis` (lateral-wall robusto 8.24.0, verso da `capSum`) per l'**asse** (8.30.1: evita "marker coricato" col click sulla parete), **evidenzia** con sfera gialla (`replaceMakeDot`) → fase **`chooseType`** ("② scegli libreria+type", dropdown nel pannello in questa slice); `replaceOnTypeChange` con source attivo → **`replaceAutoPlaceFromSource`** = posa **automatica** (asse marker `axis_occlusal` → asse sorgente, posizione=centro, **roll arbitrario**) marker pending → fase **`posed`** ("③"): **✓ Conferma** (poi **Raffina** ICP aggancia il roll) oppure **"Allinea a 3 punti"** (`replaceStartThreePoint`). **"Avanti"** (`replaceNextScanbody`) riparte da `pickSource`. `replaceSeedClear(keepSource)` preserva il source al cambio type. `replaceGuideRender` badge ① Scanbody ② Marker ③ Posa. Nota: roll auto arbitrario → la Raffina lo aggancia se il flat è nel bacino di convergenza, sennò il ramo 3-punti. _Dropdown + Raffina/Avanti/Concludi ancora nel pannello (consolidazione finestra = 8.31.0)._

**Piazzamento — SEEDING a 3 punti (UNICO flusso di posa dal 8.35.0; 8.20.0 → robustezza 8.23.0):** i 3 punti nascono da (a) **anteprima interattiva** nella **finestra guida fluttuante** `#replacePreviewBox` (8.27.0 — 2b-3b, `position:fixed` top-level in basso a sinistra, canvas ~380px + badge passi + testo-passo + controlli; era 300px nel pannello fino a 8.26.0): secondo `THREE.WebGLRenderer` su `#replacePreviewCanvas`; `replacePreviewInit/Render/Dispose`, camera orbit col drag, mesh marker dalla cache offsettata col baricentro all'origine → `worldToLocal` sui click = frame **locale canonico**; `replacePreviewPickAt` raccoglie 3 punti **numerati** 1·2·3, sprite billboard colorati `_replaceNumSprite`, figli della mesh) e (b) **3 gemelli sulla scansione** (`replaceOnViewportClick` fase `pickScan`, sprite numerati corrispondenti + memorizza la **normale** della faccia colpita). **8.54.0 — SHIFT+CLIC** (feedback utente: ruotando il modello il clic finiva fuori posizione, perché lo scan-pick ascoltava l'evento `click` che scatta anche dopo un trascinamento): entrambi i picking (anteprima marker `replacePreviewUpH` e scansione `replaceOnViewportClick`) posano SOLO con **Shift+clic PULITO** — Shift catturato al **pointerDOWN** (`replacePickDownShift` / `shiftAtDown`, non al rilascio), guardia movimento **>6px** = trascinamento → nessuna posa; listener `pointerdown` scopato a `replacePlacementMode` + tasto sx (no contaminazione cross-workflow). Trascinare (senza Shift) ruota e non posa mai; clic senza Shift → solo hint. Testi-guida → "SHIFT+CLIC". **Posa (asse robusto 8.24.0 — NON più Kabsch puro):** `replacePlaceFromSeed` prende l'**asse di inserzione dalla geometria del cilindro scanbody** via `replaceEstimateCylinderAxis` (8.24.0, slice 2b-1.5): per un cilindro le normali sono ⊥ all'asse → asse = autovettore con autovalore **minore** di `Σ area·nnᵀ` (riuso `misICP_jacobi3`), pass 1 grezzo + pass 2 sola-parete (|n·asse|<0.35); **indipendente da dove cadono i 3 click** (fix "marker coricato" del seed 8.23.0: con punti sulla parete la media normali-click era radiale → asse radiale). Verso occlusale da `capSum` (normali cap-like) → `(cB − centro scan)` → media normali-click; fallback legacy `findScanbodyCenter` sotto le 8 facce. L'asse locale del marker da `axis_occlusal`; i 3 punti danno **posizione + roll** via **Procrustes 2D** nel piano ⊥ asse. **Anteprima live + Conferma:** non finalizza subito → marker `replacePending` grigio semi-trasparente + **`✓ Conferma posa`** (`replaceConfirmSeed`) / **Ricomincia** (`replaceSeedClear`); fase `pendingConfirm`. **Rifiuto terne quasi collineari** (area < 0.08·lato_max²). Macchina a stati `replaceSeed {phase: idle|pickMarker|pickScan|pendingConfirm, markerPts, scanPts, scanNormals, markerDots, scanDots}` (**8.40.0**: rimosse le fasi auto `pickSource`/`chooseType`/`posed` e gli stati `sourceCenter`/`sourceAxis`/`sourceDot`). Per-marker `seedMarkerPts`/`seedScanPts`/`axisDir`/`position` (**seme per la Raffina 2b-2**). Reset `_hardResetReplace`. Stato `replace*` separato da `sost*`.

> **UX guida del piazzamento (8.19.0 — slice 2b-1.1, evoluta a 8.23.0):** I **3 punti del seme** (anteprima + scansione) sono **sprite numerati 1/2/3** (`_replaceNumSprite`, `CanvasTexture` billboard colorato), ceduti al marker piazzato in `dotMeshes[]` alla conferma e rimossi+disposti (con texture) via `_replaceDisposeDot` in `replaceClearScene`/`_hardResetReplace`/`replaceDeletePlaced`. **8.43.0 — slider TAGLIO sorgente** (`#replaceTrimSlider` verticale accanto al canvas, `#replaceTrimCol`): accorcia il CAD sorgente dall'origine x0/y0/z0 lungo `axis_occlusal` tenendo il **cap esposto** (`replaceTrimGeoAlongAxis`/`replaceGeoAxialRange`/`replaceOnSrcTrim`, stato `replaceSrcTrim`); anteprima live (swap `replacePreviewMesh.geometry`, marker fermo); in `replacePlaceFromSeed` il taglio si applica a `geoSrc` → MADRE visibile + `p.srcGeo` (Raffina campiona il cap), FIGLIO intero. **Hover dot ("calamita") RIMOSSO in 8.42.0** (feedback utente: "scomoda e grossolana"): tolti `replaceOnViewportHover` + listener `mousemove`, `replaceHoverDot`, `replaceHideHoverDot`, e gli orfani `replaceMakeDot` + `REPLACE_DOT_COLOR` (`REPLACE_DOT_R` resta per gli sprite). I 3 clic sulla scansione si fanno ora **liberamente** (cursore a croce, nessun indicatore che insegue il cursore). **8.56.1** (feedback utente: "il punto si posiziona lontano dal clic" — causa diagnosticata NON come zoom ma come **inerzia camera**): i controlli camera hanno un flag `noInertia` (default off); durante le fasi di posa (`pickMarker`/`pickScan`/`pendingConfirm`) `replaceSeedUpdateUI` imposta `controls.noInertia = _placing` → nel `controls.update()` la velocità di rotazione residua viene **azzerata** invece che decadere (damping 0.12) → la vista si **ferma subito al rilascio**, così il clic dei 3 punti cade dove miri (non sulla vista che deriva per inerzia). Ripristinato a `false` su `selectWorkflow` (cambio workflow a metà posa) e all'uscita dalla posa (idle via `replaceSeedUpdateUI`). NB: il picking MUA di Analizza non mostrava il problema perché aggancia il clic al centro scanbody (`findScanbodyCenter`, assorbe l'errore); il 3-punti usa il punto grezzo. **8.59.2 → REVOCATO in 8.59.3** (era una REGRESSIONE): l'8.59.2 cambiò l'NDC in `offsetX/clientWidth` assumendolo invariante allo zoom — **misurato falso** in Chromium: con `body.style.zoom=1.30` `clientX`/`rect`/`offsetX` sono in spazio VISUAL (zoomato) ma `clientWidth/clientHeight` restano UNZOOMED → `offsetX/clientWidth` sbagliava di 1.30×. **8.59.3** ripristina la formula storica `((clientX-rect.left)/rect.width)` (corretta: `clientX` e `rect` entrambi visual); resta solo `camera.updateMatrixWorld()` prima del raycast (difensivo). **8.59.5 — CAUSA REALE trovata** (diagnosi console A/B con l'utente, Chrome): camera coerente (round-trip `unproject→project` = identità, `projInv-dev`=0), viewport pieno, nessun disallineamento aspect/size; MA il **raycast su `replaceMesh` restituiva `hits[0].point` FUORI dal raggio di ~0.5-1mm** (`distToRay` 0.4-1.0mm → ~20-40px di offset). `updateWorldMatrix` sulla mesh non aiuta (non è matrice stantia); nessuna BVH → è il raycast nativo (bridge r169) che calcola male il `.point` su mesh grandi. Il `.distance` è corretto: il punto **sul raggio** alla distanza del colpo (`raycaster.ray.at(hits[0].distance, …)`) ricade ESATTAMENTE sotto il cursore (misurato `onray-offNDC`=0.0000). **Fix**: `replaceOnViewportClick` usa `ray.at(distance)` per il dot E per il seme (`scanPts`) invece di `hits[0].point`. L'anteprima marker usa un'altra mesh → era già precisa. `onViewportClick` (MUA)/`sostOnViewportClick` INVARIATI (agganciano al centro → mascherano comunque). **Guida**: div statico `#replaceGuide` nel `#panelReplace` + messaggio `#replaceStatus` in fase di placement.

> **Albero scena (layersPanel) in `replace` (8.26.0, slice 2b-3a):** ora **visibile** come negli altri workflow. `replaceRebuildTree` (gemello di `sostRebuildTree`) popola il pannello scena **condiviso** `#layerTree`/`#layersPanel`: riga **Scansione** (visibilità `replaceToggleScanVisibility` + colore `setSceneObjectColor('replacescan')` + opacità `replaceSetScanOpacity`/`replaceScanUI`) + per ogni marker **#num** (visibilità `replaceTogglePlacedVisibility` + colore `setSceneObjectColor('replace:'+num)` + RMSD + elimina `replaceDeletePlaced`). Guardia `analysisMode==='replace'` (albero condiviso: non clobbera gli altri workflow). Il ramo `replace` di `selectWorkflow` ora imposta `layPan='flex'` + `replaceRebuildTree()` (era `'none'` in 8.18.0–8.25.0). Refresh agganciato a `replaceRebuildPlacedList` (posiziona/elimina/raffina/conferma), `replaceLoadScanToScene` (la scansione compare), `selectWorkflow`. La lista `#replacePlacedList` **resta** nel pannello per il flusso (additivo). `setSceneObjectColor` (8.9.0) estesa coi target `replacescan`/`replace:`+num. _Prossimo: 2b-3b finestra guida grande, 2b-3c cut-view in sezione su pulsante._

### Pannelli a visibilità per-id (non per-classe)

Oltre alle classi, `selectWorkflow` commuta `style.display` di numerosi pannelli **per id** (non per classe). Da tabellare integralmente in Fase 2 nella sezione "Stato globale e classi di visibilità". Variabili raccolte in testa alla funzione ([4995-5005](../backend/static/syntesis-analyzer-v3b.html#L4995)): `panelAngleList`, `panelAxisInfo`, `panelExport`, `panelMisurareICP`, `panelClinicalStatus`, `panelMuaList`, `panelScanLoad`, `layersPanel`, `misurareStage`. Più due gestiti fuori da quel blocco: `panelSostituire` (mostrato nel ramo sostituire [5130-5131], nascosto in uscita [5029-5030]) e **`panelScanbodyType`** (gestione centralizzata aggiunta in 8.4.5 [5014-5015]: visibile solo in analizza/accoppia). **8.73.0**: rimossi `panelMisurareList`/`panelMisurareCompare` (Comparator v7 eliminato).

---

## Vista: Home / splash (`/` → `synthesis-home.html`)

Splash **pubblica** (`FileResponse`, nessun gate; fallback a `/vedere` se il file manca, [main.py:145-154](../backend/main.py#L145)). **Redesign DARK** (8.6.0): tema scuro (`--dark #0F1923`) + bordo perimetrale animato. **Layout "una schermata" (8.6.2)**: il contenuto vive in `.viewport` (`position:fixed`, `inset:22px` = dentro la cornice, `overflow-y:auto`) → lo scroll resta DENTRO la cornice; su desktop 16:9 le misure sono in `vh`/`clamp` → logo + hero + 4 card stanno tutti in `100vh` senza scroll; sotto `900px` il layout diventa verticale con scroll naturale (sempre dentro la cornice). **Allineamento desktop ampio (8.6.4)**: logo spostato in `.hero-left` (primo elemento) + `.hero{align-items:start}` → top logo = top immagine; logo più grande (`clamp(70px,12vh,124px)`); `.page{max-width:1600;justify-content:center}` → margini simmetrici (sx=dx, alto=basso); `.hero{flex:0 0 auto}`; eyebrow rimosso; mobile/desktop-basso con `justify-content:flex-start` (invariati). Statica/vanilla, CSS inline. Righe = `synthesis-home.html`.

| Elemento | Riferimento | Destinazione / effetto |
|---|---|---|
| Bordo animato | `.synt-frame` (div `position:fixed`, `inset:18px`, `pointer-events:none`, `z-index:9999`) | cornice perimetrale **cava** (8.6.1: `mask-composite:exclude` → interno **trasparente**, il contenuto sotto resta visibile; in 8.6.0 il riempimento `--dark` opaco copriva la pagina) a conic-gradient (rosa/viola/arancio), angolo `--synt-sa` animato via `@property` + `@keyframes syntSpin` 4s linear infinite; **overlay fisso** (fermo allo scroll), **non blocca i click** |
| Logo | `<img class="logo-img" src="/static/synthesis-logo.png">` (**in `.hero-left`**, primo elemento; `height:clamp(70px,12vh,124px)`, 8.6.4) | PNG nero → reso **bianco** da `filter:invert(1) brightness(1.9)` (verificato: pixel opachi 100% neri, niente aloni). Top allineato all'immagine. Nessun suffisso "ICP" |
| Hero testo | **logo** + `.headline` (con `.accent` blu) + `.lead` — eyebrow rimosso in 8.6.4 | titolo "Dove la connessione **implantare diventa misura.**" + paragrafo di presentazione |
| Hero immagine | `<img class="hero-img" src="/static/assets/padova-17_001.jpeg">` dentro `.hero-img-wrap` | dente reale → mesh; tenuta in **card chiara** (`#F0F1F5` + ombra/glow) che la stacca dal fondo scuro |
| Card **Vedere** | `<a class="tool-card" href="/vedere">` + SVG occhio | → `/vedere` |
| Card **Analizzare** | `<a class="tool-card" href="/analizzare">` + SVG orologio | → `/analizzare` (workflow analizza, default) |
| Card **Misurare** | `<a class="tool-card" href="/analizzare?wf=misurare">` + SVG grafico | → `/analizzare` deep-link workflow misurare |
| Card **Sostituire** | `<a class="tool-card" href="/analizzare?wf=sostituire">` + SVG cubo | → `/analizzare` deep-link workflow sostituire |

Gating: home **pubblica**. **8.78.0 — rollout gate COMPLETO** (chiuso il sospeso 2026-05-29): `/vedere`, `/analizzare` e `/dashboard` gated da `syn-gate.js` (anti-flash + `/auth/me`; non autorizzato → `/accedi`, poi ritorno al deep-link — la query `?wf=` è preservata da `rememberDeepLink` ([syn-gate.js:49](../backend/static/ds/syn-gate.js#L49))); `/accedi` pubblica; `/gestione` protetta in pagina (admin). Le 4 SVG sono le stesse del menu WorkFlow ([1271-1306] di v3b).

## Vista: Analizzare (`/analizzare` → `syntesis-analyzer-v3b.html`)

App monolite (~3.87 MB). 4 workflow interni commutati da `selectWorkflow(wf)` ([4958](../backend/static/syntesis-analyzer-v3b.html#L4958)). Tutte le righe di questa sezione si riferiscono a `syntesis-analyzer-v3b.html`.

### Chrome persistente — barra menu (sempre visibile)

| Elemento (etichetta) | id / selettore | Evento | Funzione | Stato letto/scritto | Effetto a valle | Righe | Note |
|---|---|---|---|---|---|---|---|
| Logo header | `<img class="logo-img">` [981] | — | — | — | — | [981] | **8.84.0 (Fase 1)**: `src` da data-URI B64 (74KB inline) → `/static/assets/logo.png`; pixel identici (gate MD5) |
| Menu **File** | `#fileMenu` | onclick | `toggleFileMenu` | — | apre dropdown | [1218] | |
| → Nuovo | `.file-menu-item` | onclick | `newCase` | `scanMesh`, `muaObjects` | reset caso (confirm se c'è stato) | [1222] | |
| → Importa | | onclick | `inputScan.click()` | — | apre file dialog STL | [1226] | delega a `#inputScan` |
| → Salva (JSON) | | onclick | `saveCase` | `muaObjects` | scarica `.json` | [1231] | |
| → Esporta (PNG+JSON) | | onclick | `exportCase` | `scanMesh` | screenshot PNG + json | [1236] | |
| → Impostazioni | | onclick | `openSettings` | `envSettings` | apre modal impostazioni (tab Ambiente / Macchine / Interfaccia / Algoritmo-expert). **8.86.0 (F5)**: le 36 fn del pannello (open/close/cancel/save, tab, ambiente/render/palette/fresatore/controlli/zoom) vivono in `ds/syn-env.js`; lo STATO (`envSettings`/`millerSettings`/`controlsSettings` + load) resta nel monolite (§IMPOSTAZIONI-RUNTIME) | [1242] | |
| ↳ Impostazioni → **Interfaccia → Controlli 3D (mouse)** | `#ctrlSpeed_rotate` / `#ctrlSpeed_pan` / `#ctrlSpeed_zoom` + Ripristina | oninput/onclick | `onControlsSettingChange('rotate'/'pan'/'zoom', v)` / `resetControlsSettings` | `controlsSettings` (moltiplicatori) + localStorage `syntesis_controls` | **8.82.0**: 3 slider 0.3×–2.5× (default 1.0×) per rotazione/pan/zoom camera; applicano subito via `applyControlsSettings` su `CONTROLS_BASE {0.8/0.6/1.2}`. Popolati da `syncControlsSettingsUI` in `openSettings`; applicati all'avvio in `init` dopo la creazione di `controls`. Bugfix incluso: `panSpeed` ora usato in `pan()` della OrbitControls custom (prima ignorato) | tab `#settingsTabUi` | Solo Analizzare (Vedere ha controlli propri) |
| → Accedi/Logout | `#fmAuthItem` | onclick | `syntAuthClick` | token localStorage | login/logout. **8.86.0 (F5)**: blocco IIFE login/registrazione rilocato verbatim in `ds/syn-auth-ui.js` (stessa posizione, banner §AUTH-LOGIN resta nel monolite) | [1248] | testo dinamico `#fmAuthLabel` |
| → Area personale | `#fmDashboardItem` | onclick | `window.open('/dashboard')` | — | apre dashboard | [1255] | `display:none` di default; mostrata al login da `ds/syn-auth-ui.js` (`dash.style.display='flex'`). **8.109.1 — DISABILITATA temporaneamente** (feature Dashboard non pronta, richiesta utente): regola CSS `#fmDashboardItem{display:none!important}` in un `<style>` dedicato in v3b (batte l'inline del JS, NON tocca il modulo gated). RIABILITARE = rimuovere quella riga. |
| **Reset** | `.btn` | onclick | `hardReset` | `scanMesh`, `muaObjects` | ricarica app con `?_r=Date.now()` | [1262] | feat 8.4.4 |
| Menu **WorkFlow** | `#workflowMenu` | onclick | `toggleWorkflowMenu` | — | apre dropdown | [1267] | |
| → Vedere | `[data-wf="vedere"]` | onclick | `selectWorkflow('vedere')` | — | **redirect** a `/vedere` ([4567-4571]) | [1271] | |
| → Analizza | `[data-wf="analizza"]` | onclick | `selectWorkflow('analizza')` | `currentWorkflow` | switch workflow | [1278] | `.active` di default |
| → Misurare | `[data-wf="misurare"]` | onclick | `selectWorkflow('misurare')` | `currentWorkflow` | switch workflow | [1285] | |
| → Sostituire | `[data-wf="sostituire"]` | onclick | `selectWorkflow('sostituire')` | `currentWorkflow` | switch workflow | [1292] | |
| Menu **Vista** | `#viewMenu` | onclick | `toggleViewMenu` | — | apre dropdown pannelli | [1301] | |
| → toggle pannello ×5 | `[data-vmi-target]` | onclick | `toggleViewPanel(id)` | visibilità pannello | mostra/nasconde un pannello dx | [1306-1320] | targets: `panelScanLoad`, `panelClinicalStatus`, `panelMuaList`, `panelAngleList`, `panelAxisInfo` |
| → Collassa colonna dx | `#viewMenuRailToggle` | onclick | `toggleRightColumnRail` | — | collassa colonna destra | [1322] | |
| → Mostra tutti | | onclick | `resetViewPanels` | — | ripristina pannelli | [1325] | |
| Carica file STL | `#btnLoadFile` | onclick | `syntesisOpenFileDialog` | — | apre file dialog | [1331] | **senza** classe `-only` → di fatto sempre visibile |

> **Infra pannelli (8.12.0) estratta in `backend/static/ds/syn-panel.js`** (relocazione in-place verbatim del blocco `<script>` di fine body; markup invariato, funzioni globali per gli handler inline): view-menu/pannelli/rail `toggleViewMenu` [syn-panel.js 675] · `toggleViewPanel` [504] · `hidePanel` [482]/`showPanel` [493] · `toggleRightColumnRail` [534] · `resetViewPanels` [511] · `syntesisOpenFileDialog` [284] · `applyViewState` [447]; drag/resize pannelli `synMakePanelDraggable` [169]; persistenza `syntesis_analizza_view`/`_rail` + tooltip `data-tip`. Nessun link cross-modulo. Gate `scripts/gate/panel` (browser DOM A/B, inline ≡ modulo, diff 0).

> **Doppio sistema di visibilità dei pannelli dx**: `selectWorkflow` li commuta per-workflow (matrice sotto), **ma** il menu **Vista** (`toggleViewPanel`) e i `panel-collapsible` permettono all'utente di mostrarli/nasconderli/collassarli indipendentemente. Le due cose convivono.

> **Color picker per-oggetto (8.9.0)**: nei 3 alberi scena ogni oggetto ha un `<input type="color" class="tree-color">` al posto del pallino statico. **Gestione UNICA** `setSceneObjectColor(target, hex)` [v3b ~3962] + helper `__synApplyColor` (mesh/array/group THREE), stesso meccanismo per tutti gli alberi. Target: `'scan'` (scanMesh + envSettings.scanColor), `'mua:'+idx` (m.color + mtMesh/anMesh/axisLine; scanbody resta colore-brand 1T3/OS/SR = codice clinico), `'icp:'+group` (bgA/scbA/bgB/scbB via misICP_groupMeshes; storage misICP_layerColors), `'sostscan'` (Multi-A), `'sosttype:'+key` (marker per tipo 1T3/SR/OS: gruppi sostPlaced + SOSTITUIRE_TEMPLATE_INFO). Persistenza colori "nel caso" = demandata al progetto **"apri caso"** (vedi STATO Sospesi #7).

> **Deep-link workflow `?wf=` (8.5.0)**: `/analizzare?wf=<wf>` apre direttamente quel workflow al load. Reader al `DOMContentLoaded` (dopo `setMode`, ~[5200](../backend/static/syntesis-analyzer-v3b.html#L5200)) che valida `wf ∈ {analizza,accoppia,misurare,sostituire}` e chiama `selectWorkflow(wf)` via `setTimeout(0)` (default analizza se assente/non valido). Usato dalle card **Misurare**/**Sostituire** della home. La query è preservata dal gate (`syn-gate.js` `rememberDeepLink`).

### Toolbar per-workflow (classi di visibilità)

| Elemento | id / classe | Funzione | Visibile in | Effetto a valle | Righe | Note |
|---|---|---|---|---|---|---|
| Albero Scena (**era "Livelli"**, rinominato 8.75.0) | `#btnLivelli` `.analisi-only` | `toggleLayersPanel` | analizza+accoppia | toggle albero scena (`layersPanel`); title "Albero Scena (L)". **8.89.0 (F6b)**: le 10 fn tree-view (`toggleLayersPanel`/`openLayersPanel`/`closeLayersPanel`/`rebuildTree`/`treeUnified_*`/`toggleLayer`/`toggleMuaLayer`/`toggleMuaExpand`) vivono in `wf/tree.js`; `muaExpanded` + `setSceneObjectColor`/`getGroupBadgeColor` restano nel monolite | [1337] | |
| Albero Scena (dup, **era "Livelli"** — 8.75.0) | `.sostituire-only` | `toggleLayersPanel` | solo sostituire | toggle albero scena | [1345] | `display:none` default; visibilità centralizzata in `selectWorkflow` (corretto 8.4.6) |
| **Posiziona** | `.green .analisi-only` | `startPlacement` | analizza+accoppia | `placementMode=true` → **8.58.0: SHIFT+CLIC pulito** posa il MUA (`placeMUA`); guardia movimento >6px = trascinamento → niente posa (ruotare non posa); clic senza Shift → hint. Shift/pos catturati al pointerdown via `replacePickDown*` (condivisi tra tutte le pose) | [1353] | |
| **Raffina** | `#btnRaffina` `.analisi-only` | `alignAll` | analizza+accoppia | ICP refine di tutti i MUA | [1363] | |
| Crea gruppo | `.analisi-only` | `openGroupDialog` | analizza+accoppia | apre dialog gruppi | [1373] | |
| Mostra assi | `#btnAxes` `.analisi-only` | `toggleAxes` | analizza+accoppia | toggle assi 3D | [1383] | |
| Sezione | `.analisi-only` | `openCutView` | analizza+accoppia | cutview MUA (overlay) | [1392] | |
| Sezione (mis) | `.misurare-only` | `misICP_toggleCutview` | solo misurare | cutview ICP | [1400] | `display:none` default |
| Sezione (sost) | `.sostituire-only` | `sostOpenCutView` | solo sostituire | cutview marker | [1408] | `display:none` default; visibilità centralizzata in `selectWorkflow` (corretto 8.4.6) |
| Annulla | (toolbar dx) | `undoLastMUA` | analizza (toolbar) | rimuove ultimo MUA | [1418] | |
| Menu reset | | `toggleResetMenu` | analizza | dropdown: `resetCamera` [1432], `resetGroups`, `clearAllMUA` | [1424] | |

> **✅ Leak gemello — CORRETTO in 8.4.6.** I 2 bottoni `.sostituire-only` ([1345], [1408]) hanno `display:none` inline; il ramo sostituire li mostra ([4722]), ma prima nessuno li rinascondeva uscendo (il blocco di uscita nascondeva solo `panelSostituire`, `_hardResetSostituire` non li toccava, e i rami analizza/accoppia/misurare non settavano mai `sostituireBtns`) → restavano visibili dopo una visita a Sostituire. **Fix 8.4.6**: gestione centralizzata a fine `selectWorkflow` (`querySelectorAll('.sostituire-only')` + `display` per `wf === 'sostituire'`) — nessun ramo può dimenticarli. La riga inline ridondante ([4722]) è lasciata invariata (innocua). Simmetrico al fix `#panelScanbodyType` 8.4.5.

### Pannelli destri — matrice di visibilità per workflow (`selectWorkflow`)

Valori = `style.display` impostato in ciascun ramo (`''` = visibile, `none` = nascosto). Variabili dichiarate a [4598-4610].

| Pannello (id) | analizza | accoppia | misurare | sostituire | Apertura su richiesta |
|---|---|---|---|---|---|
| `panelAngleList` | `''` [4638] | none [4660] | none [4682] | none [4707] | data-pview anglelist, collapsible |
| `panelAxisInfo` | `''` [4639] | none [4661] | none [4683] | none [4708] | data-pview axisinfo; contiene btn "Fresatura avanzata" [1730] |
| `panelExport` | none [4640] | `''` [4662] | none [4684] | none [4709] | — |
| ~~`panelMisurareList`~~ ~~`panelMisurareCompare`~~ | — | — | — | — | **RIMOSSI 8.73.0** (Comparator v7 eliminato: markup + JS + `SyntesisDB`) |
| `panelMisurareICP` | none [4643] | none [4665] | `''` [4687] | none [4712] | — |
| `panelClinicalStatus` | `''` [4644] | `''` [4666] | none [4689] | none [4713] | data-pview clinical; btn Report MUA [1687] |
| `panelMuaList` | `''` [4645] | `''` [4667] | none [4690] | none [4714] | data-pview mualist; albero MUA dinamico |
| `panelScanLoad` | `''` [4646] | `''` [4668] | none [4691] | none [4715] | data-pview scanload; dropzone `#slotScan` |
| `layersPanel` (sx) | flex [4650] | none [4671] | none [4692] | flex [4717] | albero scena |
| `panelScanbodyType` (Box A) | `''` [4617-4618] | `''` [4617-4618] | none | none | **fix 8.4.5** (ternary `analizza\|\|accoppia`) |
| `panelSostituire` | (uscita→none [4633]) | none | none | `''` [4725] | — |
| `panelFresabilita` | on-demand | — | — | — | aperto da `openFresability`, chiuso da `closeFresability`; `display:none` default [1735]. **8.88.0 (F6a)**: i 34 handler/funzioni del dominio fresabilità (open/close, macchina, modo, overlay frecce, math) vivono in `wf/fresabilita.js`; stato (`fresState`) + banner §FRESABILITA + monkey-patch `calculateAngles` restano nel monolite |
| `misurareStage` | hidden [4652] | hidden [4672] | condizionale [4697-4700] | hidden [4728] | stage caricamento ICP |

### Sotto-sezione — workflow **analizza** (`analysisMode='misura'`, ramo [4636])

Default all'avvio. Posizionamento e analisi angolare MUA. Elementi UI specifici (oltre a chrome + `.analisi-only`):

| Elemento | id | Evento | Funzione | Stato | Effetto | Righe |
|---|---|---|---|---|---|---|
| Radio Tipo scanbody 1T3/OS/SR | `#analyzeSbTypeRadio` | onchange | `setAnalyzeSbType` | `window._ANALYZE_SBTYPE` | tipo CAD (radius/searchR) per nuovi MUA | [1666-1669] |
| Report MUA (PDF) | `#btnAnalReport` | onclick | `analReport_generate` | muaObjects | genera PDF clinico. **8.90.0 (F6c)**: le 4 fn `analReport_*` (captureViews/generate/collectData/buildRecommendations) vivono in `wf/report-analizza.js`; banner §REPORT-MUA-PDF + addCornerLogo (annidata) + report Misurare restano nel monolite | [1687] | disabled finché no MUA |
| Fresatura avanzata → | `#btnOpenFresability` | onclick | `openFresability` | — | apre `panelFresabilita` | [1730] |
| Drop scansione | `#slotScan` / `#inputScan` | onclick/onchange | `inputScan.click()` / `loadScan` | `scanMesh` | carica STL scansione | [1919-1930] |
| **Fresabilità**: macchina | `#fresMachineSelect` | onchange | `fresOnMachineChange` | fresatore sel. | aggiorna max angle | [1743] |
| **Fresabilità**: modo | `name="fresMode"` | onchange | `fresOnModeChange` | mean/minimax | strategia asse | [1752-1753] |
| Indietro (fresabilità) | | onclick | `closeFresability` | — | chiude pannello | [1737] |

Consumo stato: `placeMUA` ([2753]) legge `getAnalyzeSbType()`/`getAnalyzeSbCfg()`; `alignAll` rifà ICP per-MUA (ogni MUA ricorda `mua.sbType`, [2933]).

> **Fix 8.7.1 (Posiziona in "Entrambi"):** `onViewportClick` [v3b ~2673] usa `raycaster.intersectObject(scanMesh, false)` (non ricorsivo) + guardia `hits[0].face`. Prima, in modalità "Entrambi", il raycast ricorsivo (default) colpiva per primo l'overlay wireframe (`LineSegments` figlio di scanMesh, senza `.face`) → `hits[0].face.normal` lanciava → placement morto **solo** in Entrambi (Solid/Wireframe ok). Non tocca il motore clip/stencil.

### Sotto-sezione — Taglio: clipping + stencil cap (motore r169) + pannello di prodotto (8.8.0) — **motore estratto in `ds/syn-clip.js` (8.11.0)**

Three.js **r169** (ES module + bridge `window.THREE = Object.assign({}, THREE)`, importmap+module [2026]). Il "vedere dentro" è un **clipping plane + stencil cap** sulla scansione, che **sostituisce la trasparenza** (scansione opaca + `polygonOffset(1,1)`). Clip **solo sulla scansione** (MUA interi). Pilotato dal **pannello "Taglio"** di prodotto (8.8.0), aperto da un pulsante nella view-mode bar (modello Fresabilità). Default taglio **OFF** (scansione carica intera/opaca). **Dal 8.11.0 il motore (clipping/stencil + pannello "Taglio") vive in `backend/static/ds/syn-clip.js`** (`<script src>` classico, stile `syn-render.js`/`syn-gate.js`): stato su `window.synClip*`, funzioni ri-esposte coi nomi bare → call-site del monolite e handler inline INVARIATI. Gate di equivalenza in `scripts/gate/clip` (golden verbatim ≡ modulo, 0 scostamenti).

> **Fix resa colore (8.7.2 → 8.8.1):** `renderer.outputColorSpace = THREE.SRGBColorSpace` [v3b ~2492] (8.7.2: era `LinearSRGBColorSpace` → scura/desaturata). **8.8.1**: `ColorManagement.enabled = true` [v3b ~2035] (era `false` da 8.7.0) → pipeline coerente: i colori materiale (scanColor/MUA/sfondo) decodificati sRGB→lineare in input e ri-encodati in output → niente più viraggio verso il giallo/chiaro (scanColor reso R/G 1.43 → 1.88 = colore scelto). Luci ri-tarate al **rapporto r128** 0.4:0.6:0.25 ×3 = **Ambient 1.2 / key 1.8 / fill bluastra 0.75** [v3b ~2512-2514] (8.8.0 era 1.3/1.3, ambient-gonfiato → immagine piatta) → chiaroscuro/tridimensionalità recuperati (contrasto std ~8 → ~13); direzionale:ambient = 1.5 è il limite oltre cui lo specular bianco lava la tinta. `makeGradientTexture` [v3b ~12279]: `tex.colorSpace = SRGBColorSpace` (la CanvasTexture sRGB era letta come lineare → sfondo schiarito; ora fedele). `NoToneMapping` invariato. Misurato su mock: scanMeanLum ~133, clip 0, scan + MUA non slavati.

| Elemento | id | Evento | Funzione | Effetto | Righe |
|---|---|---|---|---|---|
| Pulsante Taglio (view-mode bar) | `#btnOpenTaglio` | onclick | `openTaglio` | apre `#panelTaglio` (nasconde gli altri pannelli destri); `disabled` finché no scansione | [40031] |
| Pannello Taglio (`.panel-section`) | `#panelTaglio` | — | `openTaglio`/`closeTaglio` | swap-in stile Fresabilità; `tagState.isOpen` | [1824] |
| Taglio attivo (`.export-checkbox`) | `#tagToggle` | onchange | `tagOnToggle` | on/off clip+stencil+cap; ON forza scansione opaca | [1832] |
| Asse piano X/Y/Z (`.comp-radio`) | `#tagAxisRadio` | onchange | `tagOnAxis` | normale del piano (`synClipAxis`) | [1837] |
| Posizione (range) | `#tagPos` | oninput | `tagOnPos` | offset lungo l'asse (`synClipPos`); bounds da `synClipDiag` | [1847] |
| Inverti lato (`.export-checkbox`) | `#tagFlip` | onchange | `tagOnFlip` | nega la normale (`synClipFlip`) | [1853] |

Handler pannello e **motore** in **`backend/static/ds/syn-clip.js`** (estratto da v3b in 8.11.0; nomi ri-esposti su `window`, call-site INVARIATI): `openTaglio` [syn-clip.js 119] / `closeTaglio` [134] (clone di open/closeFresability, swap pannelli destri, NON tocca `analysisMode`); `tagSyncUI` [145] (allinea i controlli ai globali del clip); `tagForceScanOpaque` [163] (taglio ON → scansione opaca + slider opacità 100% via `rebuildTree`). I controlli settano i globali del **motore clip** (`window.synClipEnabled/Axis/Pos/Flip`) e chiamano `synUpdateClipPlane` [70] — **motore INVARIATO**: `synMakeStencilGroup` [43] (back/front incr/decr, renderOrder 1), `synPositionCap` [60], `synRebuildClip` [87] ((ri)build chiamato da `loadScanFile` [v3b 2696] e `rebuildScanMeshGeometry` [v3b 12205],[12243]; abilita `#btnOpenTaglio`). Cap plane renderOrder 1.1 (`clearStencil` in `onAfterRender`), mesh visibile renderOrder 6.

> **Convivenza "opacità comanda" (8.8.0 — debito 8.7.0 CHIUSO):** opacità <100% (`treeUnified_setScanOpacity` [4124]) o ghost (`treeUnified_ghostAll` [4154]) **disattivano** il taglio scrivendo `window.synClipEnabled = false` (trasparenza e clip+cap non coesistono → cap mai rotto) — è il motivo per cui lo stato del motore resta su `window`: così questi call-site esterni sono rimasti INVARIATI nell'estrazione 8.11.0. Riattivare il taglio dal pannello forza la scansione opaca + slider opacità a 100%. Lo slider opacità torna a fare **solo opacità**.

### Sotto-sezione — workflow **accoppia** (`analysisMode='accoppia'`, ramo [4658])

Esporta/confronta accoppiamenti. Condivide la toolbar `.analisi-only` con analizza (incluso Posiziona → quindi consuma `_ANALYZE_SBTYPE`). Pannelli propri:

| Elemento | id | Evento | Funzione | Effetto | Righe |
|---|---|---|---|---|---|
| Export: Scanbody/Matematica/Analogo | `#expSB`/`#expMT`/`#expAN` | checkbox | (lette da `exportComponents`) | selezione componenti | [1769-1771] |
| Esporta STL | `#btnExportComponents` | onclick | `exportComponents` | scarica STL componenti | [1774] |
| ~~Lista accoppiamenti + Comparator v7~~ | `#panelMisurareList`/`#panelMisurareCompare` (lista+refresh, upload src/tgt, radio `compMis`, `#btnOpenComparator`) | — | — | **RIMOSSO in 8.73.0** (scelta utente, audit 2026-07-02): il Comparator v7 era un tool ESTERNO (github.io) con ricaricamento manuale degli STL, sorpassato dal Confronto ICP in-app (`panelMisurareICP`). Eliminati markup, JS (`misurareState`/render/selezione/upload/`openComparatorV7`/`COMPARATOR_V7_URL`) e layer `SyntesisDB` (IndexedDB TTL 24h, esisteva solo per questo flusso; via anche il salvataggio in `exportComponents` e la promessa '24 ore' nella export-note). `_escHtml` conservato (condiviso). CSS `.acc-*`/`.slot*`/`.comp-radio` orfani → pulizia Fase D | — |

### Sotto-sezione — workflow **misurare** (`analysisMode='misurare'`, ramo [4678])

Confronto ICP fra 2 STL, viewport dedicato (`misICP_mountViewport`). Pannello `panelMisurareICP` [1826]. **8.93.0 (F6f 1/3)**: le 59 fn del dominio §MISURARE-ICP (parse/ICP/seed/`misICP_run`/`misICP_onPick`/`misICP_reset`/`misICP_mountViewport`/render — vedi elenco tombstone §WF-MIS-ICP nel monolite) vivono in `wf/misurare-icp.js` (`<script src>` in testa dopo `wf/replace.js`); gli handler restano bare-global invariati. Lo STATO misICP_* + costanti MIS_* + banner §MISURARE-ICP/§ASSE-CILINDRO-CONN restano nel monolite. **8.94.0 (F6f 2/3)**: le 41 fn del report (`misICP_generateReport`/`misICP_renderClinicalPDF`/`misICP_renderCalibrationPDF`/`misICP_renderExcel`/§MISURARE-PDF/§REPORT-PIPELINE/§CERTIFICATO-TARATURA) vivono in `wf/misurare-pdf.js`; resta nel monolite `_synScaricoConoImg` (preload img). **8.95.0 (F6f 3/3, CHIUDE la Fase 6)**: le 23 fn viz (label 3D `misICP_createLabels`/`misICP_updateLabels`, cutview `misICP_openCutview`/`misICP_drawCutview`, albero `misICP_showTree`/`misICP_applyLayerVis`/§MISURARE-VIZ) vivono in `wf/misurare-viz.js`; resta lo STATO misICP_labels/layerColors/cutZoom + banner §MISURARE-VIZ. Il dominio mis ha ora **0 fn nel monolite** (tutte 123 in wf/misurare-{icp,pdf,viz}.js). Gli handler restano bare-global invariati. Tabella dettaglio (id/handler/righe markup — le righe JS delle fn sono ora nei file wf/):

| Elemento | id | Evento | Funzione | Effetto | Righe |
|---|---|---|---|---|---|
| Carica STL A / B | `#misInputA` / `#misInputB` | onchange | `misICP_onPick(event,'A'/'B')` | carica mesh A/B | [1838-1848] |
| Esegui allineamento ICP | `#misBtnRun` | onclick | `misICP_run` | lancia ICP | [1849] | disabled finché 2 mesh |
| Ricomincia | `.mis-reset-btn` | onclick | `misICP_reset` | reset misurare | [1859] |
| Scarica report ▾ | `#misBtnReport` | onclick | `misICP_toggleReportMenu` | dropdown report | [1860] |
| → Clinico / Taratura / Analisi / Excel | | onclick | `misICP_generateReport('clinico'/'taratura'/'analisi'/'excel')` | genera report. **8.66.5**: Excel (`misICP_renderExcel`) ora multi-foglio con nuovo foglio **"Diagnostica"** (sistema/processo: versione, motore centraggio/asse, RMSD; + decomposizione **centro-vs-asse** per cilindro: capZ, centroide dev, cap-baricentro A/B + dev, connessione, angolo asse, verdetto CENTRO/ASSE) — label pulsante aggiornata ".xlsx multi-foglio (incl. Diagnostica)" | [1863-1866] |
| Cutview ICP: select / chiudi / reset albero | `#misCutSelect` / `.mis-cut-close` / `.mis-tree-reset` | onchange/onclick | `misICP_openCutview` / `misICP_closeCutview` / `misICP_resetTreeDefaults` | gestione cutview | [1515-1530] |
| Albero ICP: visibilità / opacità layer | `#layChk*` / `#laySld*` | onchange/oninput | `misICP_applyLayerVis` / `misICP_applyLayerOp` | toggle/opacità bgA,scbA,bgB,scbB,labels | [1543-1608] |
| **Connessione** `[beta 8.64.0; gestione 8.64.2]` | `#layChkConn` / color `misICP_setConnColor` / `#laySldConn` (gruppo Overlay) | onchange/oninput | `misICP_applyLayerVis('conn',…)` (vis) · `misICP_setConnColor` (colore SOLO matematica, `userData.misConnMat`; marker A/B invariati) · `misICP_applyLayerOp('conn',…)` (opacità, label `#layValConn`) | toggle/colore/opacità: marker-origine A/B + linea-leva + geometria matematica alle connessioni; `misICP_groupMeshes('conn')`=`misICP_connMeshes` | riga albero [~1659]; render `misICP_renderConnections` |
| Etichette 3D (label + leader-line) | `#layChkLabels` | onchange | `misICP_applyLayerVis('labels',…)` → flag `misICP_labelsVisible` (8.64.2: spegne anche la **leader-line SVG** `#labelLines` linea+pallino, gated in `misICP_updateLabels`) | toggle label HTML + leader | [1651-1657] |

> **Connessione clinica in Misurare (beta 8.64.0).** Oltre alla deviazione al **centroide** di volume, il workflow misurare calcola e mostra (accanto, non al posto) la deviazione al **punto di connessione** = `centroide − (capZ − δ)·asse_capward` (capZ da `window.SYN.scanbody`, tipo auto-rilevato dal raggio). Helper dopo `misICP_axisAngleDeg`: `misICP_detectSbType` / `misICP_orientCapward` / `misICP_connectionPoint`. Calcolo nel loop pairs (`p.connA/connB`, `p.connD3um`…); riga **CONNESSIONE** nella tabella per-cilindro del PDF (`misICP_pdfDrawCylinderPage`). Disegno in scena + nodo albero `conn` via `misICP_renderConnections` (additivo, in try/catch; cleanup in `misICP_renderPerCylinder`/`misICP_reset`/dispose-workflow). Razionale e validazione su id 2770: la connessione è il datum clinico vero ma oggi **lever-dominata** dall'asse (44µm vs 8µm al centroide; 8µm con asse perfetto). Orientamento: **OS/1T3** (8.64.1) → cap occlusale = estremo con PIÙ area di facce piatte (`misICP_orientCapward`, validato); connessione = capZ all'estremo opposto = verso l'impianto. **SR (8.67.1)** → il CAD SR è Z-invertito (`flip X 180`), i due dischi sono GEMELLI per area (6.20≈6.23), quindi la regola per-area ribaltava la connessione su ~metà marker (#4/#5/#6); nuovo helper `misICP_orientCapwardSolid` (call-site `misICP` ~6967, branch `_sb.type==='SR'`; OS/1T3 invariati) orienta verso il **disco PIENO** (cap occlusale rmin~0.025 vs base/sede-vite 0.146, 5.8× netto su tutti e 6), fallback al disco singolo (scan reale, base aperta). **Gate validato:** SR+OS+1T3 sullo stesso impianto → connessioni coincidenti **<1µm** (template MarkerOS/MarkerSR/1T3, origine condivisa a (0,0,0)). **8.69.0 (datum = ORIGINE + geometria reale):** la MISURA per-marker passa dal centroide all'**ORIGINE** = `centroide − L·asseCapward`, L per-tipo dal CAD (`MIS_ORIGIN_OFFSET` [~6790]: SR 3.786 / OS 5.574 / 1T3 8.146 mm = lo (0,0,0) condiviso dei CAD IPD; override `p.d*`/`p.d*um` nel blocco connessione [~7042]; allineamento globale resta sui centroidi, offline id2161 ±1µm; **score globale invariato**, usa RMSD ICP). Il punto-connessione È l'origine (supera l'interim 8.68.3 delta-da-A). La **geometria** disegnata in Misurare passa dalla `MATEMATICA_B64` generica (al cap) alla geometria **reale** `IPD.AB-SR-01-ZI` servita come **asset statico** (`backend/static/conn/`, mount `/static`), disegnata all'origine in orientamento nativo (STL origine Z=0=piattaforma, +Z=cap; `_misLoadConnGeo` async+cache + re-render [~7227]); fail-soft. Analizza/placeMUA INVARIATO (usa ancora `MATEMATICA_B64`).

### Sotto-sezione — workflow **sostituire** (`analysisMode='sostituire'`, ramo [5110])

Sostituzione scan body con ICP. Pannello `panelSostituire` [1965]:

| Elemento | id | Evento | Funzione | Stato | Effetto | Righe |
|---|---|---|---|---|---|---|
| Drop scansione partenza | `#sostSlotScan` / `#sostInputScan` | onclick/onchange | `sostInputScan.click()` / `sostOnScanPicked` | — | carica STL partenza. **8.80.3**: anche il **drop globale sul viewport** (listener `document` 'drop', v3b ~r.2445) ora è **workflow-aware** e instrada a `sostOnScanPicked` quando `analysisMode==='sostituire'` (prima cablato su `loadScanFile`→`scanMesh`, invisibile all'albero Sostituire); analogo per `replace`→`replaceOnScanPicked`, altrimenti `loadScanFile`. **8.80.4**: in `misurare` il drop globale NON carica (hint "usa gli slot A/B" via `misICP_showError`; prima doppio caricamento in `scanMesh` Analizza) e `misICP_onDrop` fa `stopPropagation` | [1972-1975] |
| **Radio "Scansione di partenza" 1T3/SR/OS (Box B)** | `#sostSourceRadio` | onchange | `sostOnSourceChange` | `sostSourceTemplate`, `sostActiveTemplate` | tipo marker presente (allineamento) | [1978-1981] |
> **Upload custom — RIMOSSO in 8.81.1.** La riga storica (`#sostInputCustom` / `sostOnCustomPicked`, template custom) era uno stub del prototipo 2026-04-23 mai agganciato al motore di posa e irraggiungibile da UI da quello stesso giorno (il trigger era il radio `#sostTemplateRadio`, eliminato in `a4268a8`). Catena eliminata integralmente (markup + JS + reset). L'esigenza è coperta da Replace-iT + librerie `/gestione`.
| + Posiziona | `#sostBtnPlace` | onclick | `sostStartPlacement` | — | placement marker; **8.58.0: SHIFT+CLIC pulito** posa (`sostOnViewportClick`→`sostPlaceTemplate`), guardia movimento >6px = rotazione + hint senza Shift; aggiunta guardia `.face` (come Analizza). **8.80.3**: testo istruzione del pannello (`panelSostituire` ~r.1680) reso esplicito su **SHIFT+CLIC**. **8.91.0 (F6d)**: le 47 fn del dominio Sostituire (`sost*`/`_sost*`: placement, motore robusto centro+asse, export STL, albero, cutview) vivono in `wf/sostituire.js`; lo stato `sostMesh/sostPlaced/sost*` + banner §SOSTITUIRE restano nel monolite | [1988] |
| Raffina | `#sostBtnRefine` | onclick | `sostAlignAll` | — | ICP sostituzione — il point-ICP **centra** soltanto; l'**asse** del marker viene da un fit lateral-wall della parete scansionata. Gate motore (**8.14.0**): `auto` (default) = lateral solo se `sostSourceTemplate==='SR'`; `lateralwall` = sempre (era incondizionato in 8.13.0); `cap` = mai (→ asse dal point-ICP). Fallback `R·seed` se <8 triangoli parete — vedi `sostAlignAll` [38426]/[38607] | [1989] | disabled di default |
| Esporta STL | `#sostBtnExport` | onclick | `sostExportSTL` | `_sostExportPending` | apre **`#sostExportDialog`** (nome file precompilato) → Conferma scarica col nome scelto (sanificato, `.stl` auto); Annulla non scarica | [1996] | `display:none` default; dialog nome file (8.4.7) |

> **Box B vs Box A**: Box B (`sostSourceTemplate`) = tipo di marker **già presente** nella scansione di partenza, per la registrazione (`SOSTITUIRE_TEMPLATE_INFO`, `BBOX_LOCAL`, `T_ROOT` — [34803]/[38085]/[38084]). NON è ridondante con Box A (`_ANALYZE_SBTYPE`, che guida `placeMUA` in Analizza). Vedi fix 8.4.5 e 8.4.6 sopra.

> **Modalità render globale in `sostituire` (8.71.3, fix):** la barra `#vmBar` (solid/wireframe/both) ora raggiunge anche le mesh di Sostituire — prima la vista Reticolo/Both restava solid. `sostApplyRenderMode()` [v3b ~36669] applica la modalità a `sostMesh` + alle Mesh dentro ogni `p.groups[k]` (guardia `o.isMesh` → le `THREE.Line` dell'asse restano intatte); invocata da `applyRenderModeToScene` [**ds/syn-env.js** dall'8.86.0 (F5)] e in coda a `sostRebuildTree` [~39904] (i marker piazzati dopo la scelta vista ereditano la modalità). Gemello di `replaceApplyRenderMode`.

> **Resize renderer sul viewport (8.71.3, fix):** collassando la colonna destra (rail) il `.viewport` (`flex:1`) si allarga via CSS ma **senza** un `resize` della finestra → il canvas restava con la larghezza vecchia (banda vuota). Un `ResizeObserver` sul `#viewport` [v3b ~2709, dopo il listener `window.resize`] ridimensiona `renderer`+`camera.aspect` a ogni cambio del box (anche durante la transizione CSS `.25s` di `.panel`). Canvas unico condiviso → vale per tutti i workflow.

---

## Vista: Vedere (`/vedere` → `syntesis-icp-vedere.html`)

Home di default (redirect da `/`). Viewer 3D multi-formato con misura, forme, annotazioni e sezione. ~8245 righe. **Wiring**: l'header usa `onclick` inline; la toolbar del viewer è cablata in JS (78 `addEventListener`). Gli handler reali dei bottoni toolbar sono **tracciati per-bottone** nella tabella sotto (nome funzione + riga del binding, verificati con `grep -n`). Righe = `syntesis-icp-vedere.html`.

### Header (inline)

| Elemento | Evento | Funzione | Effetto a valle | Righe |
|---|---|---|---|---|
| Home (nuova sessione) | onclick | `goHomeNewSession` | reset + home | [942] |
| Menu File | onclick | `toggleFileMenu` | dropdown | [948] |
| → Svuota scena | onclick | `clearScene` | rimuove le mesh | [953] |
| → Importa | onclick | `filePicker.click()` | file dialog | [958] |
| → Impostazioni | onclick | `openVedereSettings` | modal impostazioni | [964] |
| → Accedi/Logout | onclick | `vedereAuthClick` | login/logout | [971] |
| → Area personale | onclick | `window.open('/dashboard')` | apre dashboard | [978] · **8.109.1 (Vedere v8.0.7-refactor) DISABILITATA**: regola CSS `#fmDashboardItem{display:none!important}` nel foglio inline (riabilitare=rimuovere) |
| Menu WorkFlow → vedere/analizza/misurare/sostituire | onclick | `selectWorkflow(...)` | vedere = resta; analizza/misurare/sostituire = **redirect** a `/analizzare` | [990-1011] |

### Toolbar viewer (bottoni id, handler JS-wired via `addEventListener`)

| Elemento | id | Handler reale → riga binding | Cosa fa | Righe markup |
|---|---|---|---|---|
| Aggiungi file | `#btnPick` | `pickFiles` → [2727] | apre il file dialog (`filePicker.click()`, [2726]; input [1028]) | [1020] |
| Aggiungi (dup) | `#btnAdd` | `pickFiles` → [2728] | come btnPick: apre il file dialog | [1038] |
| Reset | `#btnReset` | arrow → [2800] | svuota scena: `clearAllMeasures`+`clearAllCircles`+`clearScene` | [1024] |
| Home camera | `#btnHome` | `fitCameraToScene` → [2807] | inquadra tutta la scena (fit camera) | [1104] |
| Sposta | `#btnMoveMode` | `setTransformMode('translate')` → [2808] | gizmo traslazione sull'oggetto selezionato | [1046] |
| Ruota | `#btnRotateMode` | `setTransformMode('rotate')` → [2809] | gizmo rotazione | [1049] |
| Deseleziona | `#btnDeselect` | `deselectLayer` → [2810] | deseleziona l'oggetto | [1052] |
| Righello | `#btnRuler` | arrow → [5732] | toggle misura distanza (`setRulerActive`; spegne cerchio) | [1056] |
| Angolo | `#btnAngle` | arrow → [5736] | toggle misura angolo (`setAngleActive`; spegne cerchio/righello) | [1059] |
| Forme Cerchio/Quadrato/Esagono/Ottagono/Torx | `#btnCircle`/`#btnSquare`/`#btnHex`/`#btnOct`/`#btnTorx` | `bindShapeButton(id,shape)` → [5811-5815] (def [5801]) | toggle annotazione forma (`setCircleActive` con la forma scelta) | [1066-1078] |
| Sezione | `#btnSection` | arrow → [7050] | se sezione attiva → `removeSection`; altrimenti `setSectionPlaceMode` | [1082] |
| Gomma | `#btnEraser` | arrow → [5746] | toggle gomma topologica (`setEraserActive`; spegne cerchio/righello/angolo) | [1085] |
| Pulisci righelli | `#btnRulerClear` | arrow → [5790] | `clearAllMeasures` + `clearAllCircles` | [1098] |
| Modalità render solid/wireframe/both | `[data-action="render-mode"][data-mode]` | **delegato**: `body.addEventListener('click')` [7376] → case `render-mode` [7975] | imposta `visual.renderMode` del layer via `SynRegistry.update` | [7694-7696] |
| Pannello sezione (PiP): chiudi/max/swap/righello/fit | `#secCloseBtn`/`#secMaxBtn`/`#secSwapBtn`/`#secRulerBtn`/`#secFitBtn` | `removeSection` [7060] / `toggleSectionPipMaxi` [7063] / `swapSectionPip` [7064] / arrow `setSectionRulerActive` [7065] / arrow `fitSectionCameraToContent` [7068] | controlli del PiP di sezione | [1129-1143] |
| Modal impostazioni Vedere | `#vsmClose` / `#vsmOk` | `close` | chiude il modale (creato lazy da `openVedereSettings`, aperto da File→Impostazioni [964]) | — |
| ↳ Impostazioni Vedere → **Controlli 3D (mouse)** | `#vCtrlSpeed_rotate` / `#vCtrlSpeed_pan` / `#vCtrlSpeed_zoom` + Ripristina | oninput/onclick | `vOnControlsSettingChange('rotate'/'pan'/'zoom', v)` / `vResetControlsSpeed` | `vControlsMult` + localStorage **`syntesis_controls`** (CONDIVISO con Analizza) | **8.83.0**: 3 slider 0.3×–2.5× (default 1.0×), stessa feature di Analizza 8.82.0 e stessa preferenza salvata. Vedere usa **TrackballControls** con `SPEED_BASE {3.0/2.0/1.2}` + turbo Shift ×2; effettivo = base × moltiplicatore, turbo ×2 sopra. Tutte le assegnazioni via `vApplyControlsSpeed(turbo)` (init + keydown/keyup/blur del turbo), `vSyncControlsUI` popola gli slider in `openVedereSettings`. Il placeholder "preferenze in arrivo" è ora reale | pannello `#vedereSettingsModal` |

> **✅ "Primo click a vuoto" su `#btnPick` — RISOLTO in 8.4.8.** Sintomo (confermato runtime): al primo click il file dialog si apriva e si richiudeva subito; al secondo restava. Causa: `#btnPick` aveva **DUE** handler che chiamavano entrambi `filePicker.click()`:
> - `onclick` inline nel markup ([1020]: `onclick="document.getElementById('filePicker').click()"`)
> - `addEventListener('click', pickFiles)` ([2727], con `pickFiles` = `filePicker.click()` [2726])
>
> → due `.click()` sincroni sull'input file per un solo click utente → la seconda chiamata annullava il dialog appena aperto (flicker open→close). `#btnAdd` ([1038], pannello layer) non ha l'`onclick` inline (solo `addEventListener` [2728]) → single trigger, e infatti funzionava: coerente col fatto che il sintomo era **specifico di `#btnPick`**. (`filePicker.value=''` [2732] è nel handler `change`, non c'entra.) **Fix (8.4.8)**: rimosso l'`onclick` inline da [1020]; `#btnPick` ora ha il **solo** `addEventListener` [2727] → single trigger (un solo `filePicker.click()`), coerente con `#btnAdd`/`#btnReset`.

## Vista: Dashboard (`/dashboard` → `syntesis-dashboard-v1.html`)

Area personale (~3992 righe). Sidebar a tab (`switchTab` [1461]) + sezioni. Diverse azioni sono in **righe generate dinamicamente** (template string JS). Righe = `syntesis-dashboard-v1.html`.

| Elemento | Evento | Funzione | Effetto | Righe | Note |
|---|---|---|---|---|---|
| Vai all'app | onclick | `location.href='/analizzare'` | apre analyzer | [935],[964] | |
| Logout | onclick | `doLogout` | logout | [936] | |
| Tab sidebar | onclick | `switchTab('analyses'/'projects'/'files'/'contacts'/'cloud'/'profile'/'leaderboard'/casi-*/…)` | cambia sezione | [975-1043] | ~17 voci |
| Filtro analisi attive/archiviate | onclick | `switchFilter('active'/'archived')` | filtra | [1052-1053] | |
| Nuovo progetto | onclick | `openProjectCreate` | modal | [1064] | |
| Filtro progetti | onclick | `switchProjectFilter(...)` | filtra | [1066-1067] | |
| Nuova cartella condivisa | onclick | `openCreateSharedFolder` | modal | [1096] | riga aggiornata 8.80.4 (shift +5 da #inviteBanner) |
| Nuovo contatto | onclick | `openContactCreate` | modal | [1116] | riga aggiornata 8.80.4 |
| Leaderboard filtri | onclick | `LBdash.filterBy('tipo',…,this)` | filtra classifica | [1187-1191] | righe aggiornate 8.80.4 |
| Ruolo PRO medico/laboratorio | onclick | `selectProRole(...)` | seleziona ruolo | [1305-1310] | riga aggiornata 8.80.4 (shift da inserimenti) |
| Conferma ruolo | onclick | `confirmProRole` | salva ruolo | [1316] | riga aggiornata 8.80.4 |
| Modal preview: scarica / chiudi | onclick | `downloadPreviewFile` / `closeModal('modalPreview');cleanupPreview()` | preview file | [1389-1390] | righe aggiornate 8.80.4 (shift +63 dai modali ripristinati) |
| Modal contatto: chiudi / salva | onclick | `closeModal('modalContact')` / `saveContact` | gestione contatto | [1428-1429] | righe aggiornate 8.80.4 |
| **Modal rinomina analisi** `#modalEdit` | onclick | `closeEditModal` / `saveModal` | rinomina/note analisi | [1326-1343] | **8.80.4: markup RIPRISTINATO** (perso in 4b4c297/v7.3.9.055 con JS+bottone 'Modifica' rimasti vivi → TypeError; risolto anche ESC che non chiudeva il modal Profilo) |
| **Modal progetto** `#modalProject` | onclick | `closeProjectModal` / `saveProjectModal` + `.color-swatch` (delegato) | crea/modifica progetto | [1347-1379] | **8.80.4: markup RIPRISTINATO** (stessa storia di #modalEdit: '+ Nuovo progetto' e 'Modifica' crashavano) |
| **Banner inviti** `#inviteBanner` | onclick (injected) | `acceptInvite`/`declineInvite` via `renderIncomingInvites` | inviti cartelle condivise | [1088] | **8.80.4: contenitore AGGIUNTO** in cima a tabFiles (prima inesistente: inviti invisibili/non accettabili) |
| **(dinamico)** Analisi: dettaglio/modifica/PDF/archivia/elimina | onclick (injected) | `openDetail`/`openEdit`/`downloadPdf`/`setArchived`/`deleteAnalysis` | azioni per-analisi | [1764-1777] | iniettati a runtime; righe aggiornate 8.80.4 |
| **(dinamico)** Paginazione | onclick (injected) | `changePage(-1/1)` | naviga pagine | [1789-1791] | righe aggiornate 8.80.4 |
| **(dinamico)** Progetti: dettaglio/modifica | onclick (injected) | `openProjectDetail`/`openProjectEdit` | azioni per-progetto | [2081-2091] | righe aggiornate 8.80.4 |

## Vista: Accedi (`/accedi` → `syntesis-accedi.html`)

Pannello accesso a 3 stati (login/registrazione/attesa→autorizzato), ~468 righe. **Wiring via `addEventListener`** (no onclick inline). Righe = `syntesis-accedi.html`.

| Elemento | id | Evento | Funzione (def / binding) | Effetto | Righe markup | Righe binding |
|---|---|---|---|---|---|---|
| Tab Accedi / Registrati | `#tab-login` / `#tab-register` | click | `setTab` [263] | mostra form | [142-143] | [270-271] |
| Form login: email, password | `#form-login` (`#l-email`,`#l-pass`,`#btn-login`) | submit | handler [378] | autentica → token | [149-154] | [378] |
| Form registrazione: nome,email,org,città,tel,password | `#form-register` (`#r-name`…`#r-pass`,`#btn-register`) | submit | handler [391] | crea utente pending | [161-176] | [391] |
| Pending: Controlla adesso | `#pending-check` | click | `checkStatus` [359] | polling `/auth/me` | [194] | [417] |
| Pending: Esci | `#pending-logout` | click | `logout` [421] | logout | [195] | [418] |
| Authorized: Copia chiave | `#copy-key` | click | handler [428] | copia chiave SICP | [212] | [428] |
| Authorized: Entra in Syntesis-ICP | `#enter-app` | click | handler [437] | → `/analizzare` | [217] | [437] |
| Authorized: Esci | `#auth-logout` | click | `logout` [421] | logout | [218] | [419] |

## Vista: Gestione (`/gestione` → `syntesis-gestione.html`)

Pannello admin (~1320 righe da 8.50.0; ~2050 dall'8.109.0). **Wiring via `addEventListener`**; righe utente generate da `render()`. Righe = `syntesis-gestione.html` (i numeri pre-8.50 sono shiftati: localizzare con `grep -n`).

> **8.109.0 — Shell "Cockpit tecnico".** Il layout passa da topbar orizzontale + `.admin-tabs` (4 tab) a **griglia `body[sidebar|main]`**: `<aside class="sb">` (brand + nav `.sb-item[data-pane=utenti|librerie|archivio|crea]` con icona SVG + chip Amministrazione in fondo) e `<div class="main">` (topbar sottile con titolo/badge + `.wrap` con le 4 `<section class="tabpane">` identiche + modali + toast). `switchAdminTab`/wiring click ora su `.sb-item`; `data-pane`/`tabpane`/logica invariati. Re-skin CSS-only sui nomi-classe esistenti (nessun id/funzione rimosso; inventario identico verificato). `.admin-tabs`/`.atab` = **dead CSS** da rimuovere in pulizia dedicata.

| Elemento | id / selettore | Evento | Funzione | Effetto | Righe markup | Righe binding |
|---|---|---|---|---|---|---|
| Filtro In attesa / Autorizzati / Tutti | `[data-f]` | click | handler (→ `render`) | filtra lista | [133-135] | [372] |
| Cerca | `#search` | input | `render` | filtra per nome/città | [140] | [381] |
| Ordina | `#sort` | change | `render` | riordina | [142] | [382] |
| **(per riga)** Autorizza | (injected) | click | `onAuthorize` | genera chiave SICP, attiva utente | — | (binding per riga) |
| **(per riga)** Revoca | (injected) | click | `onRevoke` | revoca accesso | — | (binding per riga) |
| Modal: Copia chiave / Chiudi | `#m-copy` / `#m-close` | click | handler | copia / chiudi | (markup modale) | (binding modale) |

> I numeri di riga del pannello utenti sono shiftati dall'aggiunta della sezione Replace-iT (8.16.0); localizzare con `grep -n`. La logica utenti è invariata.

### Sezione "Librerie Replace-iT" (8.16.0 — ingest librerie scanbody Exocad)

Aggiunta sotto il pannello utenti, **stessa IIFE e stesso wiring `addEventListener`**, riusa `adminHeaders`/`escapeHtml`/`fmtDate`/`toast`. Backend: `admin.py` → `/admin/rit/*` (dietro `require_admin`); DB: `database.py` (tabelle `rit_marker_stl` / `rit_library` / `rit_scanbody_type`, schema in `init_db`). **NON** tocca il runtime Sostituisci né l'analisi. Nessun deploy in 8.16.0.

| Elemento | id / selettore | Evento | Funzione | Effetto | Endpoint |
|---|---|---|---|---|---|
| Input ZIP + Carica | `#rit-file` / `#rit-upload-btn` | click | `ritUpload` | upload multipart libreria — ZIP Exocad (config.xml) **o, da 8.50.0, ZIP manuale (libreria.csv + STL)**; risposta `kind:"rows"` → `ritRowsOkMsg`; 409 `confirm_needed` → `ritShowConfirm` (ri-POST con `stl_overwrite`/`lib_overwrite`/`code`) | `POST /admin/rit/libraries` |
| Tabella librerie | `#rit-rows` (render) | — | `ritLoad` → `ritRender` | lista import_name/keyword/display/fornitore/n.type/stato | `GET /admin/rit/libraries` |
| **(per riga)** Dettagli | (injected) | click | `ritDetail` → `ritRenderDetail` | root-params (read-only) + tabella **componenti EDITABILE (8.51.0)**: per riga Nome / Ruolo (—/Madre/Figlio/Entrambi) / ENG editabili + checkbox Attivo + **Salva** (`ritSaveType`→PATCH) + **Elimina** (`ritDeleteType`→DELETE); marker read-only; preview PNG; sottotitolo per `source` | `GET /admin/rit/libraries/{id}` · `PATCH`/`DELETE …/types/{type}` (+ `/preview`) |
| **(per riga)** toggle Attiva/Disattiva | (injected) | click | `ritToggle` | attiva/disattiva libreria | `PATCH /admin/rit/libraries/{id}/active` |
| Dialog conflitto keyword | `#rit-overlay` / `#rit-conflict` | click | `ritShowConflict` / `ritCloseConflict` | da 409 `keyword_conflict`: elenca **esplicitamente** ogni libreria sovrascrivibile (import_name, caricata, n.type, uploaded_by) da `existing[]`; per-riga "Sovrascrivi" (`mode=overwrite`+`target_library_id`) o "Importa come nuova" (`mode=new`+`import_name`) | (ri-`POST` con `mode`) |

**Comportamento ingest** (server): salta `__MACOSX/` e `._*`, ignora subtype/`.sdfa`/firme RSA; **validazione bloccante** = ogni `MarkerFilename` deve esistere come STL nello ZIP (altrimenti 400, rollback totale); dedup STL per `sha256` del contenuto (`rit_marker_stl` globale); `active=FALSE` di default; `uploaded_by` = email admin dal JWT. Conflitto keyword senza `mode` → **409** con `existing[]`, nessuna scrittura.

> **Empty-state e visibilità (`.hidden`)** — gli empty-state `#rit-empty` ("Nessuna libreria importata") e `#empty` (tabella utenti), più il pannello dettaglio `#rit-detail`, si nascondono/mostrano via toggle JS della classe `.hidden` (`ritRender` `.classList.add/remove("hidden")`, `render` utenti, close/open `#rit-detail`). **Fix 8.16.1**: la classe `.hidden` non aveva regola CSS nel `<style>` → il toggle non aveva effetto visivo e `#rit-empty` restava visibile a lista popolata (e `#empty` latente, mascherato dalla tabella sempre popolata). Aggiunta `.hidden{display:none}` (dopo `.empty`): il toggle ora funziona per tutti e tre. Solo CSS, nessuna modifica JS.

### Sezione "Archivio STL" + "Crea libreria" (8.50.0 — creazione librerie dal pannello)

Due sezioni sotto "Librerie Replace-iT", stessa IIFE/wiring. Backend: `admin.py` (`_rit_resolve_files`/`_rit_write_resolved` condivisi da TUTTE le porte, `_rit_ingest_rows` CSV/editor, `_rit_build_libraries_from_rows`, `_rit_check_lock_code`); DB: `database.py` (NUOVE `rit_stl_asset` = **cartella unica** per nome → sha256, `rit_lock_secret` = codice lucchetto hashed pbkdf2; colonne additive `rit_scanbody_type.role`, `rit_library.source`). **Archivio UNIFICATO (8.50.0)**: anche l'import Exocad passa dalla cartella → gli STL di **tutte** le librerie (Exocad incluse) vivono lì una sola volta; i marker condivisi (0T3/1T3/2T3 = scanbody IPD usato su più marche) si riusano. Backfill idempotente in `init_db` (normalizza `marker_filename` storici a basename + popola `rit_stl_asset` dai type esistenti). **Modello "live per nome" GLOBALE**: sovrascrivere un asset ripunta i `marker_sha256` dei type di **ogni** libreria che usa quel nome (lucchetto = protezione dei master). Anteprima 3D: Three r169 via importmap+bridge `window.THREE` (stesso loader di `/vedere`), `OrbitControls`, parser STL inline `parseSTL` (binario+ASCII), `AxesHelper` + sfera bianca su origine (0,0,0). Il runtime Replace-iT (v3b) **non è toccato**.

| Elemento | id / selettore | Evento | Funzione | Effetto | Endpoint |
|---|---|---|---|---|---|
| Upload STL multiplo | `#stl-files` / `#stl-upload-btn` | click | `stlUpload` | carica in archivio; 409 `stl_conflict` → `ritShowConfirm` (per-file sovrascrivi/non caricare; bloccati → campo codice) | `POST /admin/rit/stl` |
| Tabella archivio | `#stl-rows` (render) | — | `stlLoad` → `stlRender` | file/dimensione/usato-da/lucchetto/aggiornato; empty `#stl-empty` | `GET /admin/rit/stl` |
| **(per riga)** Anteprima | (injected `.linkbtn`) | click | `stlPreviewAsset` → `stlPreviewOpen/Render` | modale `#stl-prev-overlay` con mesh + terna origine (X rosso/Y verde/Z blu) + sfera su (0,0,0) | `GET /admin/rit/stl/{name}/content` |
| **(per riga)** Blocca/Sblocca | (injected `.linkbtn`) | click | `stlToggleLock` | blocco libero (se codice impostato); sblocco chiede il codice (`prompt`), verifica server-side | `POST /admin/rit/stl/{name}/lock` · `/unlock` |
| **(per riga)** Elimina | (injected `.linkbtn.danger`) | click | `stlDelete` | confirm; il server rifiuta se `locked` (409) o `in_use` da librerie CSV/editor (409) | `DELETE /admin/rit/stl/{name}` |
| Codice lucchetto… | `#stl-code-btn` | click | `stlCodeOpen` | modale `#stl-code-overlay`: imposta/cambia (richiede attuale se già impostato); stato via `stlCodeStatus` | `GET`/`POST /admin/rit/lock-code` |
| + Aggiungi riga | `#ed-add-row` | click | `edAddRow` | nuova riga editor (marca/modello/Ø/asse/ruolo/file/nome/ENG); select file = archivio o "Carica nuovo…" (`input file` nascosto per riga, `edFiles[rowid]`) | — |
| **(per riga)** Anteprima / × | (injected `[data-ed=prev]` / `[data-ed=del]`) | click | (inline) | anteprima del file della riga (upload locale via `File.arrayBuffer()` o archivio) / cancella riga | — |
| Salva libreria | `#ed-save` | click | `edReadRows` → `edSave` | POST `rows` JSON + `stl_files` multipart; ok → `ritRowsOkMsg` + refresh; 409 `confirm_needed` → `ritShowConfirm` → ri-`edSave` | `POST /admin/rit/libraries` |
| Scarica template CSV | `#ed-template` | click | `edTemplate` | Blob client-side `libreria_template.csv` (BOM Excel, 4 righe esempio, 2 diametri) | — |
| Modale conferma | `#rit-confirm-overlay` / `#rit-confirm` | click | `ritShowConfirm` | conflitti STL per nome (checkbox per file; bloccati → `#rit-conf-code`) + librerie esistenti da sovrascrivere | (ri-POST con decisioni) |

**Schema CSV / righe editor**: `marca,modello,diametro,asse_occlusale,ruolo,file,nome,eng` — obbligatorie marca/modello/diametro/ruolo(`madre`\|`figlio`)/file; separatore `,` o `;` auto; ogni (marca,modello,diametro) → una libreria (`display` "Marca Modello ØD", `import_name`/`keyword` slug, `supplier`=marca) con ≥1 madre e ≥1 figlio; upsert per `import_name` previa conferma. Vincolo fisico: **tutti gli STL esportati sulla stessa origine** (è ciò che fa ereditare la posa al figlio).

### API pubblica di lettura `/api/rit/*` (8.17.0 — Replace-iT Passo 2a, senza UI)

Endpoint **solo backend** in `backend/main.py`, dietro `require_authorized` (admin passa; utente serve `active`+`license_key`; pending → 403), che espongono **solo le librerie `active=TRUE`**. È la superficie che il workflow Replace-iT del monolite (Passo 2b, ancora da costruire) consumerà; **nessun elemento UI in questo passo**. Le scritture restano in `/admin/rit/*` (`require_admin`). Helper in `database.py`: `rit_list_active_libraries` (NEW), `rit_get_marker_bytes` (NEW), `rit_get_library_detail`/`rit_get_library_image` (estesi con `active_only`).

| Metodo · rotta | main.py | Ritorna | 404 / note |
|---|---|---|---|
| `GET /api/rit/libraries` | [rit_public_list_libraries](../backend/main.py) | librerie attive: id, import_name, keyword, display, supplier, supplier_version, n_type | solo `active=TRUE`; omette metadati admin |
| `GET /api/rit/libraries/{id}` | [rit_public_library_detail](../backend/main.py) | root-params (rotation_lock_count, ref_rotation_offset, axis_occlusal) + connection_id/supplier + `types[]` (display, keyword, marker_filename, marker_sha256, click_center, axis_asymmetric, is_eng, ord) | **404** se non esiste o non attiva; omette uploaded_by/uploaded_at/logo |
| `GET /api/rit/markers/{sha256}` | [rit_public_marker_stl](../backend/main.py) | bytes STL marker, `application/octet-stream`, `ETag=sha256` + `Cache-Control: immutable` | **404** se sha non valido (non 64-hex) o assente; `If-None-Match` → **304**; servito per sha puro (nessun filtro attivazione) |
| `GET /api/rit/libraries/{id}/preview` | [rit_public_library_preview](../backend/main.py) | preview PNG (`image/png`) | **404** se non attiva o senza preview |

---

## Funzioni chiave referenziate

| Funzione | File / Riga (def) | Cosa fa (1 frase) | Chiamata da |
|---|---|---|---|
| `selectWorkflow` | v3b [4958] | Commuta workflow analyzer e gestisce visibilità pannelli/toolbar | menu WorkFlow, `setMode` [5187] |
| `placeMUA` | v3b [3000] | Posiziona un MUA (legge `_ANALYZE_SBTYPE` → radius/searchR) | click viewport quando `placementMode` [2739] |
| `startPlacement` | v3b [2716] | Attiva `placementMode` | btn Posiziona [1365]; tasto P (titolo bottone — **nessun handler keydown attualmente cablato**) |
| `alignAll` | v3b [3159] | Raffina con ICP tutti i MUA | btn Raffina [1375] |
| `loadScan` / `loadScanFile` | v3b [2693] / [2694] | Carica STL della scansione in `scanMesh` | `#inputScan` [2078], File→Importa |
| `hardReset` | v3b [4600] | Ricarica l'app con cache-bust `?_r=` | btn Reset [1267] |
| `newCase` | v3b [4551] | Reset del caso corrente (confirm se stato) | File→Nuovo [1227] |
| `setAnalyzeSbType` / `getAnalyzeSbType` / `getAnalyzeSbCfg` | v3b [2115] / [2134] / [2135] | Scrive/legge `_ANALYZE_SBTYPE` e cfg CAD | radio Box A [1723-1725]; `placeMUA` |
| `sostStartPlacement` / `sostAlignAll` | v3b [35040] / [38299] | Placement marker / Raffina ICP nel workflow Sostituire; Raffina centra (point-ICP) + asse da fit lateral-wall della parete; gate `syntesis_axis_engine` (**8.14.0** `auto`=SR-only via `sostSourceTemplate`; era incondizionato in 8.13.0) | btn `#sostBtnPlace` [1988] / `#sostBtnRefine` [1989] |
| `onAxisEngineChange` | v3b [3429] | Scrive `syntesis_axis_engine` a **3 stati** (`'auto'` default \| `'cap'` \| `'lateralwall'`) dal radio **"Motore asse cilindro"** (3 opzioni, tab Algoritmo, modal Impostazioni); gate del motore asse per `findScanbodyCenter` (placement), `sostAlignAll` (Raffina) e `misICP_cylAxis` (report). **8.14.0** `auto`: lateral solo per SR (placement via `opts.radius`~2.03 / Raffina via `sostSourceTemplate` / report via altezza `H`>2.4mm), cap per 1T3/OS | radio "Motore asse cilindro" [39859-39864], tab Algoritmo (modal `openSettings` [13296]) |
| `onSostCenterChange` / `sostRobustCenter` | v3b [3448] / [37656] | **8.15.0 (Replace-iT, beta)** Centraggio Sostituire **click-invariante** dietro flag `syntesis_sost_center` (**8.96.0: default `'robust'`** \| `'legacy'` fallback, **1T3/OS/SR dal 8.63.0**, prima SR-only): `sostRobustCenter` rifinisce il **centro XY** con ri-crop cilindrico ITERATO della parete attorno all'asse lateral-wall + circle-fit algebrico (kasa) → indipendente dal punto di click; mantiene livello disco; gate copertura `SOST_MIN_COVERAGE_DEG`=140° con **fail-soft** al centro legacy. **8.63.0**: esteso a tutti i tipi (beta opt-in, default `'legacy'` invariato); per **1T3/OS** l'asse del motore globale è cap-media → nuovo helper `_sostLocalWallAxis` ricava un **asse lateral-wall LOCALE** (min-eigenvector di Σ area·n·nᵀ sulla parete via `misICP_jacobi3`, **non** tocca `synAxisUseLateral`/Analizza/report) usato solo qui; asse raffinato + centro robusto applicati SOLO se `applied` (copertura≥140°); **triplo fail-soft** (parete<12 tri → cap-media; copertura<140° → legacy; flag default legacy). Innesto in `sostPlaceTemplate` (sotto flag, tutti i tipi, try/catch). Helper `synSostCenterRead` [3423]. **8.63.4**: per **1T3/OS** il centro passa al **baricentro del CAP** (faccia occlusale, sempre catturata → niente fail-soft anche su parete povera) via `_sostCylFitInvariant` (fit cap+parete iterato a punto fisso) → **ripetibilità** (export bit-identici, stesso file → 0). **8.65.0** (accuratezza, caso Tara id 2770): il cap-baricentro è ripetibile ma slitta **lateralmente** per-marker (cap asimmetrico su marker inclinato → 3-12µm dall'asse vero); nel branch robust 1T3/OS, dopo `_sostCylFitInvariant`, si rifinisce il **solo centro laterale** (piano ⊥ asse) con `sostRobustCenter` (Kasa parete) — che sposta il centro solo nel piano ⊥ asse, quindi livello disco (axial dal cap) e asse restano intatti; gate copertura ≥140° con **fail-soft al cap-baricentro** (parete povera → resta 8.63.4, no regressione); diag in `_sostInvLastReason` (`+kasaXY(cov=…)`/`(kasaXY skip …)`). SR invariato (centro Kasa diretto da 8.15.0). **8.66.0** (caso Tara id 2770, diagnosi gate-validata: geometrie sostituto/sorgente bit-identiche → il residuo ~8µm è **puro tilt d'asse OS** 0.1-1.2°, roll irrilevante; il centro è già giusto = Kasa no-op, MD5 OS-20≡OS-21): nuovo helper **`_sostDiscPlaneAxis`** (fit PCA del **piano del cap occlusale** → normale = asse) usato **solo per OS** nel branch robust, dopo il Kasa; l'asse di `_sostCylFitInvariant` viene dalla **parete** che per l'OS corto (h=1.1mm) è poco osservabile → tilt, mentre il cap largo+piatto dà una normale meglio condizionata. Imposta **solo la direzione** dell'asse (roll-free per costruzione; centro/livello invariati). Guardia: correzione >5° → fail-soft asse cap-fit; null se cap<8 facce; diag `+discAxis`/`(discAxis skip)`. **8.66.1 REVOCATO** (collaudo live peggiore: RMSD 8.4→10.3, #3 10→17µm — la normale del piano-disco non è più accurata della parete per l'asse OS; fitting d'asse da singola feature esaurito ~0.5°): chiamata `_sostDiscPlaneAxis` disabilitata, branch robust OS torna al comportamento 8.65.0 (cap-fit + Kasa); helper marcato **dead code**. **8.66.2** (cleanup cosmetico): l'helper `_sostDiscPlaneAxis` + il blocco commento DEAD CODE sopra di esso sono stati **rimossi fisicamente** (net −59 righe, nessun cambio di logica; `_sostCylFitInvariant` e `sostPlaceTemplate` intatti). Indagine chiusa (workflow multi-agente): una registrazione roll-free azzera il Tara ma solo per mesh-identità sintetica (scan reali 67-467µm; il centroide area-pesato slitta ~67µm sotto tassellature diverse) → il limite reale è la **metrica di Misurare**, non la posa. **8.66.3** (RIAPERTA su osservazione utente "#4 sempre 14µm, stessa mesh di #5"; diagnosi gate-validata: i 6 marker sono mesh bit-identica + estimatori d'asse equivarianti → il wall-eigenvector geometria-seeded ha errore **0.08° UNIFORME**, ma il piazzamento reale dà 0.3-1.0° **non-uniforme correlato alla verticalità** (r=-0.7, #4 dritto peggio): `findScanbodyCenter` (seed) inizializza l'asse dalle normali del **click** + ref mondo `[0,0,1]`, e su OS corto il fit click-seeded di `_sostCylFitInvariant` non rimuove il bias → **#4≠#5 è un artefatto fixabile, non fondamentale**): nuovo helper **`_sostGeomWallAxis`** (rough = normale-disco dominante del cluster, indip. dal click; poi min-eigenvector parete = asse) usato **solo per OS** nel branch robust dopo il Kasa → asse **equivariante** seed-indipendente; solo direzione (roll-free), centro/livello invariati; guardia 5° → fail-soft; diag `+geomAxis`/`(geomAxis skip)`. PENDING collaudo live (atteso residuo uniforme + #4 in calo). **8.66.7** (vera causa Tara #4≠#5, foglio Diagnostica 8.66.5/.6): il residuo è il **CENTRO** (cap-baricentro ~= centroide, ~18.7µm su #4), non l'asse; offline tutte le candidate di centro/asse sono **equivarianti/uniformi** → la non-uniformità live veniva dall'**ORDINE**: la Kasa (`sostRobustCenter`) fitta il centro nel piano ⊥ asse usando l'asse cap-fit **contaminato** (seed click+ref-mondo non rimosso su OS corto) → piano inclinato → centro off ~18µm sul marker più dritto. **FIX = riordino**: per OS, `_sostGeomWallAxis` (asse pulito) viene eseguito **PRIMA** di `sostRobustCenter`, così la Kasa fitta nel piano ⊥ asse pulito → centro equivariante. Solo riordino di blocchi esistenti; SR/1T3 invariati. Default `'legacy'` = 8.14.0 invariato (additivo, ramo legacy bit-identico). **8.69.6** (PIANO DEL CAP, flag SEPARATO `syntesis_sost_cap_plane`='on', **nessun radio UI** — solo localStorage): sul **ramo SR** (che dall'8.68.2/8.69.5 usa `_sostGeomWallAxis` tissue-robust per l'asse), dopo il Kasa si calcola un **candidato cap-plane** che raffina l'**asse** dalla normale del **cap occlusale** (feature più pulita): `_sostCapPlaneOn` [~37956] (legge il flag) → `_sostCapPlaneFit` [~37966] (crop sfera 6mm; selezione cap |n·asse|≥0.65 / |assiale|≤0.18mm / radiale≤R+0.07; piano robusto **IRLS origine-only** via `misICP_jacobi3` + peso Tukey) → `_sostCapAnchoredPose` [~38021] (asse-cap + riancoraggio assiale al piano-cap + ri-Kasa `sostRobustCenter` nel nuovo piano, dietro **gate intrinseci severi**: n≥150 / areaFrac≥0.70 / cov≥300° / planeRMS≤35µm / lamRatio≥0.55 / dAng≤0.35° / nWall≥600 / trust-region centro≤35µm → altrimenti **ROLLBACK a 8.69.5**). **PER-MARKER, no cross-marker** (direttiva utente). Innesto nel ramo SR di `sostPlaceTemplate` [~38112], `try/catch` fail-soft. **DEFAULT OFF** = 8.69.5 al bit. Reframe: il residuo dopo 8.69.5 è l'ASSE (centroide-mesh già ~1.85µm; gate foglio = punto funzionale cap+5mm·asse). Offline gate funzionale 8.58→3.07µm; diag `_sostInvLastReason` `+capPlane(dXX)` / `(capPlane rollback)`. **8.69.7** (SHADOW LOG, richiesta utente per togliere l'attrito del flag): il cap-plane e' ora **calcolato SEMPRE** per SR e scritto nel **CSV PosaDiag** a ogni Sostituire, indip. dal flag; **applicato** alla posa solo col flag (default OFF = 8.69.5 al bit). `_sostCapAnchoredPose` [~38024] ritorna SEMPRE un oggetto diag (mai null) {applied, reason(ok|no-cap-fit|cap-gate|wall-gate|trust-region), dAng, planeRMS, cov, areaFrac, lamRatio, n, nWall, centerShift, axis, center}; reason CSV `+capPlane(dXX)`=applicato / `[capShadow dXX]`=calcolato-ma-flag-off / `(capPlane <motivo>)`=respinto. **16 colonne nuove** nel CSV diagnostico (`capApplied,capReason,capDAng,capPlaneRMSum,capCov,capAreaFrac,capLamRatio,capN,capNWall,capCenShiftUm,capAxX/Y/Z,capCenX/Y/Z`) → header+row 40→56 (verificato 56==56). Math/gate identici a 8.69.6. **8.69.8** (METHOD C, scelta utente, opt-in `syntesis_sost_method_c`='on'): stima CONGIUNTA centro+asse 5-DOF (3 traslazioni + 2 tilt asse, roll congelato) con obiettivo semantico cap-point-to-plane + wall-radiale + coerenza-normali, Tukey IRLS, Levenberg-Marquardt scritto a mano. Funzioni `_sostMCOn` [~38047] / `_mcMedian`/`_mcTukey`/`_mcBasis`/`_mcSolveLin`/`_mcDot` / `_sostMethodCFit` [~38078, crop 6mm + bande cap/wall + LM+IRLS] / `_sostMethodCPose` [~38149, 6-DOF→R_fit poi 5-DOF, gate]. Init = posa cap-plane. Innesto ramo SR [~38266] dopo cap-plane (lo supera quando applicato). SHADOW: calcolato sempre e loggato; applicato solo col flag (default OFF = 8.69.7 al bit). Gate: R_fit∈[R±0.15], ncap≥150, nwall≥600, trust centro≤60µm / asse≤0.5° → rollback. reason CSV `+methodC(cXXaYY)` / `[mcShadow cXXaYY]` / `(methodC <motivo>)`. **13 colonne nuove** (`mcApplied,mcReason,mcCenShiftUm,mcAxDeg,mcRfit,mcNcap,mcNwall,mcAxX/Y/Z,mcCenX/Y/Z`) → header+row 56→69 (verificato 69==69). Port JS validato 1:1 vs Python; ~30ms/marker. **8.69.9** (scelta utente, OS migliorabile): Method C **esteso al ramo OS/1T3** di `sostPlaceTemplate` [~38234, dopo il kasa], init dalla posa cap-fit (`_sostCylFitInvariant` _cen/_ax) — niente cap-plane per OS (SR-specifico). Stesso flag/shadow/gate; le 13 colonne `mc*` del CSV ora si popolano anche per OS/1T3. Bande OS verificate su sintetico pulito (ncap=1420, nwall=44720, Rfit=1.779 nominale); trust asse 0.5° forse stretto per OS → axDeg loggato anche in rollback per taratura live. Validazione gate-vs-exocad OS rinviata (file utente non in frame col log) | radio **"Motore centraggio Sostituire"** [39893-39907], tab Algoritmo (modal `openSettings` [13296]) — **8.72.0**: tab Algoritmo `.expert-only` (area Expert) |
| `onReplaceICPChange` / `replaceICPRead` | v3b | **8.48.0 (Replace-iT, beta)** Motore ICP della Raffina dietro flag `syntesis_replace_icp` (`'p2point'` default = produzione, Kabsch \| `'p2plane'` beta): in beta `_replaceDoRefine` usa **point-to-plane** (sistema 6×6 `_replaceSolveLin` Gauss+pivot + Rodrigues `_replaceRotFromOmega`, normali per-triangolo della scansione) + **rifiuto-normali** sorgente↔scan (gengiva/back-face, `REPLACE_NORMAL_REJECT_COS`=0.3); reti di sicurezza: guardia anti-passo + fallback Kabsch. Default IDENTICO alla produzione (toggle off → Kabsch senza rifiuto). Validazione sintetica non conclusiva (cilindro liscio determina male il laterale = test debole) → beta da validare A/B su scan reale | radio **"Motore ICP Replace-iT"** tab Algoritmo |
| `onSostRaffinaChange` / `sostAlignAll` (Raffina) | v3b [2438] / `wf/sostituire.js` | **Motore Raffina Sostituire** dietro flag `syntesis_sost_raffina` a **3 stati**: `'p2plane'` (**DEFAULT dal 8.99.0** — instrada la Raffina attraverso **Method C** `_sostMethodCPose`: ICP semantico point-to-plane (cap assiale + parete radiale + coerenza normali, LM+IRLS) che **disaccoppia** disco/cilindro; per-marker, trust-region interna centro 60µm/asse 0.5°, gate ncap≥150/nwall≥600; **fallback per-marker** al Kabsch bilanciato se non applica = pavimento a comportamento 8.97.0; bypassa il fix-asse lateral-wall perché Method C fitta già l'asse) \| `'balanced'` (8.97.0, ora **storica** — ICP punto-a-punto Kabsch pesato, peso cap = nBody/nCap → Σcap=Σparete) \| `'legacy5x'` (peso cap 5× fisso, tarato OS). Il default (`_raffinaMode` in sostAlignAll + `savedRa` in `syn-env.js switchSettingsTab`) è `'p2plane'` in **entrambi** i punti. Helper `_sostRaffDiag` + **7 colonne CSV post-Raffina** (`raffEngine/raffCenShiftUm/raffAxDeg/raffVsMcUm/raffMcReason/raffMcNcap/raffMcNwall`; `raffVsMc`=distanza dall'optimum Method C, ~0=converge; `raffEngine`=p2plane|p2plane-fallback:reason|balanced|legacy5x). Verificato headless su SR sintetica: 0.00µm zero-error; flip a default supportato da mcApplied=ok sui 6 SR reali + fallback-floor. Chi ha scelto ESPLICITAMENTE balanced/legacy5x lo mantiene (localStorage) | radio **"Motore Raffina Sostituire"** (Punto-a-piano `checked` / Bilanciata / 5×cap) [~6527-6567], tab Algoritmo (modal `openSettings`) — `.expert-only` |
| `replaceRefineServer` / `POST /api/rit/refine-icp` | v3b `replaceRefineServer`; backend [main.py `rit_refine_icp`] + [icp_engine.py `refine_point_to_plane`] | **8.59.0 (Replace-iT)** Pulsante **Raffina+** (`#replaceBtnRefineSrv`): opzione PARALLELA di raffinamento posa via ICP **point-to-plane FULL-RES SERVER** (`cKDTree` O(N·logM) → niente cap 400/1500 del client). Frontend: estrae madre full-res + crop scansione (riuso logica `_replaceDoRefine`, intatta), POST Bearer, applica stesso delta 4×4, mostra RMSD/coppie/iter/ms (A/B vs Raffina client). Backend: `require_authorized`, Pydantic `RefineICPRequest`, executor+timeout `ICP_TIMEOUT_SECONDS`, cap `MAX_REFINE_SRC`=8000/`MAX_REFINE_TGT`=20000; motore solve 6×6 linearizzato + fallback Kabsch, trimming mediana×2.5 + rifiuto-normali, peso 5× cap, Rodrigues `_rot_from_omega`. Validato offline (residuo fit ~1µm su test mesh 1:1; sul reale CAD-vs-scan il residuo è ~10-20µm, comunque < client). **8.59.1** (hardening da revisione avversariale): replaceRefineServer disabilita anche `#replaceBtnRefine` durante la chiamata + snapshot posa (`_poseSnap`, scarto risposta se la posa cambia) → anti doppio-apply; **guardia anti-delta** (rot >8° o trasl >1.5mm → scarta, posa invariata); status "residuo fit" (non "RMSD"); endpoint 400/500 con messaggio generico + log server-side (no `{e}` raw). | btn `#replaceBtnRefineSrv` [flusso replace] — **8.72.0**: `.expert-only` (area Expert) |
| `misICP_cylAxis` | v3b [6574] | Asse cilindro per il report Misurare ICP; raffina dalla parete (minor eigenvector di Σ area·nnᵀ) con `syntesis_axis_engine='lateralwall'`, o **8.14.0** `'auto'` se cilindro alto (`H`>2.4mm = SR); default `'auto'`, `'cap'` = cap-PCA storico | `misICP_run` → report clinico |
| `sostExportSTL` / `openSostExportNameDialog` / `closeSostExportNameDialog` / `confirmSostExport` / `_sostDoExport` | v3b [38761] / [38800] / [38808] / [38814] / [38830] | Export STL Sostituire con dialog nome file (8.4.7): valida + nome default → apre `#sostExportDialog` → Annulla / Conferma (sanifica) → `_sostDoExport` scarica | btn `#sostBtnExport` [1996]; `#sostExportDialog` |
| `sostOnSourceChange` | v3b [34892] | Scrive `sostSourceTemplate`/`sostActiveTemplate` (Box B) | radio Box B [1979-1981] |
| `_hardResetSostituire` | v3b [4894] | Reset stato Sostituire all'uscita | `selectWorkflow` [4986] |
| `misICP_run` | v3b [6883] | ICP di confronto fra 2 STL (Misurare). **8.67.2 FIX allineamento**: il pre-align brute-force (`misICP_bruteForcePreAlign`) calcola il miglior fit Kabsch ma l'orchestrazione (~6932) lo applicava solo se `applied` (una permutazione ≠ identità batte l'identità). Con i cluster A/B nello STESSO ordine l'identità è già la migliore → `applied=false` → fit ottimo (~30µm) SCARTATO → `misICP_runICP` riparte dai centroidi GREZZI → minimo locale (RMSD scala-mm su STL congruenti, rotazione XY). Fix: `preUsable` (n∈[3..8] & rmsd finita) applica SEMPRE il best Kabsch come stato iniziale ICP + guard (scarta l'ICP se peggiora `preAlign.rmsd`) + composizione su `preUsable`. Casi già allineati invariati (best Kabsch≈identità=no-op); HD preservato. | btn `#misBtnRun` [1938] |
| `toggleLayersPanel` | **wf/tree.js (8.89.0)** | Toggle albero scena (`layersPanel`) | btn Albero Scena [1349] |
| `openFresability` / `exportComponents` / `openCutView` / `openGroupDialog` / `openSettings` | **wf/fresabilita.js (8.88.0)** / [10940] / [11536] / [12356] / **ds/syn-env.js (8.86.0)** | Pannelli/dialog: fresabilità, export STL, cutview, gruppi, impostazioni | toolbar/pannelli Analizza/Accoppia |
| `saveCase` / `exportCase` / `undoLastMUA` / `clearAllMUA` / `toggleAxes` | v3b [3730] / [4610] / [4486] / [4493] / [3681] | Salva, esporta, annulla, reset MUA, toggle assi | menu File / toolbar |
| `syntesisOpenFileDialog` | ds/syn-panel.js [284] | Apre file dialog STL | btn Carica file [1343] |
| `switchTab` | dashboard [1461] | Cambia sezione della dashboard | sidebar [975-1043] |
| `render` / `onAuthorize` / `onRevoke` | gestione [262] / [332] / [343] | Render lista / autorizza (genera chiave) / revoca | filtri, righe utente |
| `setTab` / `checkStatus` / `logout` | accedi [263] / [359] / [421] | Tab login/reg / polling stato / logout | tabs, pannello pending |

> **✅ Tabella ri-verificata per riga (8.68.0 — 2026-06-23).** L'intera tabella "Funzioni chiave referenziate", lo "Stato globale e classi di visibilità" e tutte le righe sost*/replace*/Taglio della mappa sono state riallineate al file attuale via `grep -n` per-simbolo (NON con offset aritmetici): risolte insieme la **drift storica per-voce** (es. `placeMUA` ora [3000], `selectWorkflow` [4958], `onAxisEngineChange` [3429], `misICP_cylAxis` [6574]) e lo **shift +19218 righe dell'8.68.0** sul codice v3b oltre ~13419 (es. `sostStartPlacement` [35040], `sostAlignAll` [38299] + ancore parete [38426]/[38607], export Sostituire [38761]+, `replace*` [35570]–[37434], `#btnOpenTaglio` [40031]). Le citazioni puntano ora alle righe reali. NB: `tasto P` per `startPlacement` non ha più alcun handler `keydown` (resta solo il titolo del bottone). **Ancora da riallineare** (fuori dallo scope di questo passo; drift solo storica perché < 13419): le tabelle di dettaglio per-vista Analizzare/Misurare (menu File, toolbar, **matrice di visibilità** righe ~124-211 / ~234-256) e la matrice pipeline + viste Dashboard/Accedi/Gestione (~297-341). **8.68.1**: il fix `misICP_isScanbody` (+3 righe a ~6247) sposta di **+3** le citazioni v3b > ~6250 rispetto ai valori qui sopra (es. `misICP_cylAxis` ~6577, `sostAlignAll` ~38302); riallineamento minore rinviato. **8.69.6**: le 3 funzioni cap-plane (~79 righe a 37956-38033) + l'innesto SR (~9 righe in `sostPlaceTemplate`) spostano le citazioni v3b di **+79** (da `sostPlaceTemplate` ~38034, prima dell'innesto) e **+88** oltre l'innesto SR ~38118 (es. `sostAlignAll` ora ~38471, export Sostituire ~38933); i simboli **< 37956 restano invariati** (`_sostGeomWallAxis` 37896, `sostRobustCenter` 37715, `sostStartPlacement` 35099). Riallineamento per-simbolo della zona sost*/replace* rinviato (drift cosmetica preesistente, §3.4 —  niente cleanup in task funzionale a ridosso di deploy). **8.69.7** (shadow-log cap-plane): rispetto ai valori 8.69.6, `sostPlaceTemplate` ~+8 (→38042), `sostAlignAll` ~+24 (→38495), `sostExportSTL` ~+36 (→38969); `_sostCapAnchoredPose` ~+3 (comment, →38024); simboli < ~38020 invariati (`_sostCapPlaneFit` 37966, `sostRobustCenter` 37715). Riallineamento per-simbolo sempre rinviato (drift cosmetica, §3.4). **8.69.8** (blocco Method C ~129 righe): rispetto a 8.69.7, `sostPlaceTemplate` ~+129 (→38171), `sostAlignAll` ~+154 (→38649), `sostExportSTL` ~+163 (→39132); simboli < ~38040 invariati. Le citazioni sost*/replace* restano da riallineare per-simbolo (rinviato). **8.69.9** (innesto Method C OS ~18 righe a ~38234): `sostAlignAll` ~+19 (→38668), `sostExportSTL` ~+19 (→39151); simboli < ~38234 invariati. **8.70.0** (CLICK-TO-SEED Misurare): NUOVI ELEMENTI UI nel pannello Confronto ICP — pulsante `#misBtnSeed` (onclick `misICP_seedToggle`) + pannello `#misSeedPanel` (selettore tipo `#misSeedType` 1T3/OS/SR via `misICP_seedSetType`, contatore `#misSeedCount`, bottoni Allinea `misICP_seedAlign` / Annulla `misICP_seedUndo`) subito dopo `#misBtnRun` [~1938]. Funzioni nuove [~7216]: `misICP_seedToggle/seedEnter/seedPick/seedUndo/seedAlign/seedExit/cropScanbody/seedUpdatePanel` + stato `misICP_seedMode/seedTarget/seedType/seedRawTri/seedRawMesh/seedMarkers/seedCenters/seedAxes/overrideTri`. L'utente clicca i scanbody (raycaster NDC clientX/rect come Sostituire) -> `findScanbodyCenter` per-click -> crop-and-clean (`misICP_cropScanbody` cilindro R+0.7) -> `misICP_overrideTri` consumato 1-volta da `misICP_run` [~6919: triA/triB = override se presente]; il resto della pipeline auto-detection INVARIATO. SHIFT: misICP_run ~+17 (~6465), misICP_renderPerCylinder ~+132 (~7427), zona sost*/replace*/Taglio v3b ~+132 oltre ~7216 (vs 8.69.9). Auto-path invariato. **8.70.1** (REDESIGN click-seed da crop-and-clean a innesto DIRETTO): rimossa `misICP_overrideTri`/`misICP_cropScanbody`; nuovo `misICP_clickSeeds={file,centers,axes}` consumato 1-volta da `misICP_run` — `scanCentsB`=centri cliccati [~6970], `_seedAxB`=asse cliccato trasformato dall'ICP [~7056] (niente PCA su crop sporco), `bgTrisB`=intera scansione [~7138], guardia throw partB.scan [~6955]. `misICP_seedAlign` payload diretto (≥4 click); `misICP_seedPick` feedback su click non agganciato. Auto-path INVARIATO al bit (_seeds=null → guardie originali). Net ~−20 righe (rimozione crop). **8.70.2** (UX): `misICP_seedEnter` nasconde `#misurareStage` (.hidden) entrando in click-mode; `misICP_seedExit(keepState,keepStageHidden)` lo ripristina su Annulla; `misICP_seedAlign`→`seedExit(false,true)`. Solo UI. **8.70.3** (FIX): blocco connessione (~7080) — `_axBc`=asse cliccato se `trisOfBt` undefined (era crash `forEach`); `misICP_seedPick` ora richiede **Alt+clic** (`if(!ev.altKey)return`); istruzione pannello aggiornata. **8.70.4**: il modificatore del picking seed e' SHIFT (convenzione posa app 8.58.0), non Alt — `misICP_seedPick` ora `if(!ev.shiftKey && !ev.altKey)return`; istruzione 'Shift+clic'. **8.71.0** SESSION LOGGER (debug, cross-workflow frontend): `synLog(cat,msg,data)` [~2463] -> SYN_LOG+localStorage; `#synLogDot` (pallino grigio fisso in basso a sinistra) -> `synLogUnlock` (prompt password, confronto hash SHA-256 `SYN_LOG_PW_HASH`) -> `synLogDownload` (.txt). 19 punti strumentati (init/selectWorkflow/misICP_setFile/seedEnter/seedPick/seedAlign/misICP_run start+risultato+errore/window.onerror). **8.71.1** FIX `misICP_autoThresh` [~6347]: aggiunto cap fisico `MAX_CLUSTER_MM=5.5` alla soglia di clustering (`min(...,5.5)` + `min(max(...,nn[0]*1.1),5.5)`) -> scanbody mono-componente distanti 8-11mm (OS) non piu' fusi a coppie; risolve nA=3 vs nB=6. **8.71.2** FIX `misICP_renderConnections` [~7482]: orientamento connessione 3D type-aware `_connSign=(p.sbType==='SR')?-1:1` (SR `-connAxA` invariato, OS/1T3 `+connAxA` = meccanica opposta, CAD non flippato). Solo render; Diagnostica/PDF (cap-point) e misura invariate.

> **✅ Vedere — handler toolbar: RISOLTO.** Le funzioni legate ai bottoni toolbar sono ora tracciate per-bottone (nome + riga del binding) nella tabella "Toolbar viewer" della sezione Vedere.

## Stato globale e classi di visibilità

### Global JS principali (analyzer `syntesis-analyzer-v3b.html`)

| Global | Scritto da | Letto da | Riga init/chiave |
|---|---|---|---|
| `currentWorkflow` | `selectWorkflow` [4992] | rami/guardie workflow | — |
| `analysisMode` (`'misura'`/`'accoppia'`/`'misurare'`/`'sostituire'`) | rami `selectWorkflow` [5043/5065/5085/5111] | guardie `calculateAngles`/`calculateMeanAxis` [3544/3618] | — |
| `scanMesh` | `loadScanFile`, `newCase`, `hardReset` | `placeMUA`, export, stato | — |
| `muaObjects` | `placeMUA`, `clearAllMUA`, `newCase` | analisi angolare, report | — |
| `window._ANALYZE_SBTYPE` | `setAnalyzeSbType` [2115] | `getAnalyzeSbType`/`placeMUA` | init [2114] |
| `sostSourceTemplate` / `sostActiveTemplate` | `sostOnSourceChange` [34893-34894] | pipeline Sostituire ([35033],[37972],[38372]) | decl [34737]/[34768] |
| `placementMode` | `startPlacement` [2716] | click handler [2727] | — |
| `envSettings` | `openSettings`/impostazioni | render mesh, export | — |

### Classi di visibilità per-workflow (recap, `selectWorkflow`)

| Classe | analizza | accoppia | misurare | sostituire | Righe toggle |
|---|---|---|---|---|---|
| `.analisi-only` | ✅ | ✅ | ✕ | ✕ | [5053]/[5075]/[5100]/[5125] |
| `.misurare-only` | ✕ | ✕ | ✅ | ✕ | [5054]/[5076]/[5101]/[5126] |
| `.sostituire-only` | ✕ | ✕ | ✕ | ✅ | centralizzato a fine `selectWorkflow`: `''` in sostituire, `none` altrove — **corretto 8.4.6** |
| `.icon-only` | — | — | — | — | non-gating (styling) |
| `panelScanbodyType` (id) | ✅ | ✅ | ✕ | ✕ | [5014-5015] (fix 8.4.5) |

Più i pannelli per-id nella **matrice di visibilità** della sezione Analizzare.

### Note cross-vista

- `selectWorkflow('vedere')` (in `/analizzare`) e i workflow analizza/misurare/sostituire (in `/vedere`) sono **redirect** tra le due pagine: l'analyzer e il viewer sono due file/route distinti che condividono la barra WorkFlow.
- Vedere, Accedi, Gestione cablano l'interattività via `addEventListener` (non `onclick` inline); Dashboard e Analyzer usano prevalentemente `onclick` inline + righe dinamiche.

---

_Fine mappa. Stato: tutte e 5 le viste coperte; **nessuna voce DA CHIARIRE aperta**. Storico risolto: (1) handler toolbar Vedere → tracciati per-bottone; (2) "primo click a vuoto" `#btnPick` → **RISOLTO in 8.4.8** (rimosso doppio trigger: onclick inline su #btnPick, ora solo addEventListener)._

---

## Mappa strutturale del monolite (generata)

<!-- DEP-CENSUS:START (generato da scripts/dep_census.py — non editare a mano) -->

### Fotografia (8.100.0 — rigenerare con `python3 scripts/dep_census.py --write-mappa`)

| Metrica | Valore |
|---|---|
| Righe / peso | **6892** / **0.4 MB** |
| JS applicativo REALE | **5393** righe in 6 blocchi `<script>` (grezzo 5393, meno il blob B64) |
| Asset B64 embedded | **0.0 MB** in 0 righe (**0%** del file) |
| Funzioni top-level | 115 |
| Globali condivise | 146 (di cui **24** toccate da 2+ domini) |
| Export surface (handler inline) | 31 funzioni da preservare su `window` |

### Domini: dimensione e superficie di accoppiamento

| Dominio | # fn | stato scritto→altri | stato letto←altri | API inline | API cross-dominio |
|---|---|---|---|---|---|
| `mua` | 36 | 5 | 8 | 5 | 14 |
| `workflow` | 15 | 8 | 5 | 5 | 0 |
| `diag` | 12 | 0 | 0 | 4 | 5 |
| `other` | 8 | 0 | 1 | 1 | 7 |
| `export` | 8 | 0 | 1 | 5 | 0 |
| `log` | 6 | 0 | 0 | 0 | 1 |
| `chrome` | 6 | 0 | 0 | 2 | 4 |
| `fres` | 5 | 0 | 0 | 3 | 0 |
| `io` | 4 | 1 | 5 | 1 | 1 |
| `cut` | 3 | 0 | 1 | 3 | 3 |
| `bootstrap` | 3 | 2 | 5 | 1 | 1 |
| `tree` | 3 | 0 | 2 | 1 | 1 |
| `geom` | 2 | 0 | 2 | 0 | 2 |
| `scene` | 2 | 0 | 2 | 0 | 1 |
| `colorclass` | 1 | 0 | 0 | 0 | 1 |
| `find` | 1 | 0 | 0 | 0 | 1 |

### Accoppiamenti call-graph più pesanti (dominio→dominio: # funzioni)

`mua→chrome:11` · `workflow→mua:9` · `mua→cut:6` · `log→other:5` · `mua→other:5` · `io→mua:4` · `mua→geom:4` · `fres→mua:4` · `io→chrome:3` · `mua→find:3` · `workflow→chrome:3` · `mua→tree:3`

> Elenchi completi per-dominio (globali, API) in `scripts/dep_census_out.json`.
> Ordine di estrazione e meccanismi: `docs/MODULARIZZAZIONE_STUDIO.md` (strategia).
> Regola §4: rigenerare questa sezione a ogni bump MINOR e prima di ogni estrazione.

<!-- DEP-CENSUS:END -->
